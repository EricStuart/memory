# Kele-0707 development notes

Record count: 15

# Task Group: D:\hyli\kele-0707 MARA/CASSIAN episode-shot role-presence diagnosis
scope: Diagnose missing canonical shot role references by tracing live `analyze_script` Step 8 output through normalization, presence reconciliation, marker insertion, split shots, and readback. This rollout was read-only; no repair or historical backfill was performed.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse the diagnostic chain for live automated-workflow role-presence mismatches; confirm the current authenticated project, task/API-step evidence, catalog names, and persisted output before acting.

## Task 1: Diagnose MARA/CASSIAN missing episode-shot role extraction, success

### rollout_summary_files

- rollout_summaries/2026-08-18T10-05-01-xf0M-diagnose_mara_cassian_missing_shot_role_extraction.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\18\rollout-2026-08-18T18-05-01-01a01454-df66-7042-9425-13215505e964.jsonl, updated_at=2026-08-19T03:21:51+00:00, thread_id=01a01454-df66-7042-9425-13215505e964, live browser-backed diagnosis; no source or project data changed)

### keywords

- D:\hyli\kele-0707, MARA, CASSIAN BLACKTHORN, presentObjects, script_structure.step_8, req_1787103889143_omtr12hh, exact canonical-name matching, MARA（玛拉）, physical-presence-timeline.ts, 赶到, insertObjectEnterMarkers, splitShotsFromStructuredScript, shot-object-state.ts

## User preferences

- when the user asks why named roles are missing from a live automated-workflow project, trace the complete input -> LLM output -> normalization -> persistence/readback -> shot-reference chain with authenticated project evidence, not the UI alone [Task 1]
- when the investigation is read-only, do not modify code or project data; distinguish diagnosis from a future repair and rerun/backfill plan [Task 1]

## Reusable knowledge

- Distinguish global object extraction from episode/shot presence association: MARA and CASSIAN BLACKTHORN had global object/media nodes, while MARA was missing from early canonical shot role references. [Task 1]
- Raw `script_structure.step_8` output was decisive: it already included `MARA` in `presentObjects.characters` at indexes 5, 6, and 7. The first confirmed loss was post-processing in `services/worker/structure/analyze-script-structure-service.js`, whose exact canonical-name filter removed `MARA` because the catalog name was `MARA（玛拉）`. [Task 1]
- The chain to inspect is structured analysis -> `presentObjects` -> `physical-presence-timeline.ts` / `insertObjectEnterMarkers()` -> `splitShotsFromStructuredScript()` -> `shot-object-state.ts` canonical character references and UI. `shot-object-state.ts` treats a non-empty `presentObjects` category as authoritative and does not promote merely mentioned objects. [Task 1]
- `enterKeywords` contains `赶到`, but the action-subject extraction verb list omits it; the reconciler can recognize an entrance-like action yet fail to infer MARA as the subject. After any repair, historical rows require rerunning analysis and downstream split stages. [Task 1]

## Failures and how to do differently

- Symptom: an absent role looks like a provider or UI failure. Cause: later normalization can discard already-correct provider output. Fix: inspect raw API-step output before blaming the model or patching display code. [Task 1]
- Symptom: local DB evidence cannot explain the exact visible project. Cause: the target was in an authenticated remote browser context. Fix: use the signed-in workflow page as decisive evidence and keep local code/DB results as supporting evidence. [Task 1]
- Symptom: a regex reproduction demonstrates the `赶到` gap. Cause: that confirms a secondary deterministic-rule mismatch, not the first loss. Fix: preserve causal order and label any unimplemented patch or historical repair as unverified. [Task 1]

# Task Group: D:\hyli\kele-0707 personal WeChat authentication and production delivery
scope: Personal-only WeChat Open Platform OAuth login/binding, production-origin environment rendering, and ordinary Chinese commit/rebase/push delivery; studio/admin identity separation and live QR OAuth remain explicit boundaries.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse the personal OAuth contracts and deployment checks in this checkout, but re-check current provider credentials, callback origin, and remote state before production or push operations.

## Task 1: Replace Authing with direct personal WeChat Open Platform OAuth, success

### rollout_summary_files

- rollout_summaries/2026-08-11T02-20-55-iQWt-personal_wechat_oauth_production_delivery.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\11\rollout-2026-08-11T10-20-55-019fee9f-75d1-7ae3-ac78-a8edb694a559.jsonl, updated_at=2026-08-17T03:00:59+00:00, thread_id=019fee9f-75d1-7ae3-ac78-a8edb694a559, shipped and verified)

### keywords

- 只用于个人用户, WeChat Open Platform, WECHAT_OPEN_APP_ID, WECHAT_OPEN_APP_SECRET, unionid, openid, OAuth callback, /api/personal/auth/wechat/callback, Authing

## Task 2: Render production configuration and preserve HTTPS callback origin, success

### rollout_summary_files

- rollout_summaries/2026-08-11T02-20-55-iQWt-personal_wechat_oauth_production_delivery.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\11\rollout-2026-08-11T10-20-55-019fee9f-75d1-7ae3-ac78-a8edb694a559.jsonl, updated_at=2026-08-17T03:00:59+00:00, thread_id=019fee9f-75d1-7ae3-ac78-a8edb694a559, production env and renderer regression)

### keywords

- render-prod-env, NEXT_PUBLIC_BASE_URL, https://keleaidev.12dms.com, PORT=3100, callback domain, tests/render-prod-env.test.mjs, .env.production.server-<deployment-host>

## Task 3: Rebase, commit in Chinese, and push personal WeChat delivery, success

### rollout_summary_files

- rollout_summaries/2026-08-11T02-20-55-iQWt-personal_wechat_oauth_production_delivery.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\11\rollout-2026-08-11T10-20-55-019fee9f-75d1-7ae3-ac78-a8edb694a559.jsonl, updated_at=2026-08-17T03:00:59+00:00, thread_id=019fee9f-75d1-7ae3-ac78-a8edb694a559, commit 7b030877 pushed and verified)

### keywords

- <internal-host>:3001, git credential reject, credential.interactive=never, git fetch origin main --prune, git stash pop 'stash@{0}', 7b030877, HEAD...origin/main, 0 0

## User preferences

- when the user says "只用于个人用户", keep WeChat login, registration binding, and replacement strictly in the personal portal; do not expose or associate WeChat identities in studio/admin auth [Task 1]
- when production credentials are pasted into chat, treat the AppSecret as compromised, advise rotation, and use only redacted presence/length checks; never repeat or persist the secret [Task 1][Task 2]
- when the user asks to pull latest, commit, and push, preserve unrelated work, stage only intended files, use a Chinese commit message, avoid force-push, and verify final divergence [Task 3]

## Reusable knowledge

- Direct personal OAuth uses `WECHAT_OPEN_APP_ID` and `WECHAT_OPEN_APP_SECRET`; Authing is no longer used for this flow. The start/callback contract is `/login?mode=personal` and `/api/personal/auth/wechat/callback`. Identity matching prefers `unionid`, falls back to `openid`, and can reconcile historical Authing rows through stored profile identifiers [Task 1]
- Personal-only protections remain SMS proof for registration/replacement, signed OAuth state cookies, personal session creation, one active WeChat identity per user, and session revocation on replacement [Task 1]
- Production requires `FEATURE_PERSONAL_ACCOUNT=true`, `AUTH_ENABLE_SMS_AUTH=true`, the two WeChat variables, and `NEXT_PUBLIC_BASE_URL=https://keleaidev.12dms.com`. Keep the public origin on standard HTTPS while the internal listener remains `PORT=3100`; the Open Platform callback domain is bare `keleaidev.12dms.com` [Task 2]
- Focused verification passed 17 tests with 1 DB integration skip for missing isolated DB, plus `pnpm ts-check`, `pnpm build`, and `git diff --check`. Real QR scanning was not performed [Task 1][Task 2]
- Remote delivery used `http://<internal-host>:3001/quyuan/kele.git`; clearing the stale credential for that host with `git credential reject`, then using noninteractive fetch, enabled fast-forward/rebase and push. Commit `7b030877` ended clean at `HEAD...origin/main` `0 0` with no production secrets in the commit [Task 3]
- Related skill: `skills/kele0707-git-sync/SKILL.md`

## Failures and how to do differently

- Symptom: contract tests still expect the old Authing expression. Cause: implementation intentionally moved to direct WeChat. Fix: update assertions to `WECHAT_OPEN_*` contracts [Task 1]
- Symptom: Windows executable path contains a duplicated drive prefix such as `D:\\D:\\...`. Cause: a `file:` URL pathname was passed directly to Node. Fix: convert with `fileURLToPath(new URL(...))` [Task 1]
- Symptom: rendered public callback becomes `https://keleaidev.12dms.com:3100`. Cause: renderer appended the internal listener port to an explicit HTTPS origin. Fix: test explicit public-origin handling separately and preserve standard HTTPS port [Task 2]
- Symptom: `git stash pop stash@{0}` is parsed incorrectly in PowerShell. Fix: quote the ref as `git stash pop 'stash@{0}'` [Task 3]
- Boundary: no live provider QR scan was available; do not claim end-to-end OAuth readiness beyond contract/build checks [Task 1]

# Task Group: D:\hyli\kele-0707 personal-account payment integration
scope: Build and harden the first-stage PC web payment flow for WeChat Native QR and Alipay, including signed provider recovery, merchant validation, idempotency, deployment configuration, and mock verification; refunds and real-merchant testing remain separate boundaries.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse the payment contracts and verification procedures in this checkout, but re-check current provider configuration, merchant identifiers, and product rules before production activation or refund work.

## Task 1: Audit payment readiness and define first-stage scope, success

### rollout_summary_files

- rollout_summaries/2026-08-12T07-34-48-f43W-pc_wechat_native_payment_flow_completion.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\12\rollout-2026-08-12T15-34-48-019ff4e5-2e96-7302-af11-495e7011597c.jsonl, updated_at=2026-08-12T08:11:12+00:00, thread_id=019ff4e5-2e96-7302-af11-495e7011597c, first-stage scope and readiness audit)
- rollout_summaries/2026-08-11T01-45-02-fPT5-harden_alipay_production_payment_flow.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\11\rollout-2026-08-11T09-45-02-019fee7e-9c4e-7522-b126-4077c11b4d5b.jsonl, updated_at=2026-08-12T07:49:48+00:00, Alipay readiness audit)

### keywords

- D:\hyli\kele-0707, PC website, WeChat Native, Alipay, production readiness, ALIPAY_SELLER_ID, real credentials, refund, reconciliation, manual renewal

## Task 2: Harden Alipay production payment flow, success

### rollout_summary_files

- rollout_summaries/2026-08-11T01-45-02-fPT5-harden_alipay_production_payment_flow.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\11\rollout-2026-08-11T09-45-02-019fee7e-9c4e-7522-b126-4077c11b4d5b.jsonl, updated_at=2026-08-12T07:49:48+00:00, 12/12 focused payment tests plus typecheck/build/diff check passed)

### keywords

- alipay.trade.precreate, alipay.trade.query, alipay.trade.close, RSA2, seller_id, app_id, ALIPAY_SELLER_ID, x-idempotency-key, advisory lock, same-origin, payment_orders, payment-orders/sync

## Task 3: Complete first-stage WeChat Native payment flow, success

### rollout_summary_files

- rollout_summaries/2026-08-12T07-34-48-f43W-pc_wechat_native_payment_flow_completion.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\12\rollout-2026-08-12T15-34-48-019ff4e5-2e96-7302-af11-495e7011597c.jsonl, updated_at=2026-08-12T08:11:12+00:00, 29/29 focused payment/subscription checks plus typecheck/diff check passed)

### keywords

- WeChat Native, APIv3, Wechatpay-Serial, WECHATPAY_VERIFICATION_KEYS_JSON, appid, mchid, query, close order, webhook, payment sync, render-prod-env, 29/29

## Task 4: Refine personal billing and billing-admin product configuration UI, success

### rollout_summary_files

- rollout_summaries/2026-08-14T02-40-18-PhYp-kele0707_billing_admin_ui_and_push.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\14\rollout-2026-08-14T10-40-18-019ffe24-47f3-78a2-a4b3-4b5a5a905bad.jsonl, updated_at=2026-08-14T10:45:59+00:00, thread_id=019ffe24-47f3-78a2-a4b3-4b5a5a905bad, delivered real payment-page and billing-admin refinements, then staged/rebased/pushed only the intended files)

### keywords

- personal billing, recharge products, subscription plans, 基础配置, 权益说明, 前端展示, Seedance rate, personal-product-management-panel.tsx, personal-product-preview.tsx, recharge-product-pricing.test.mjs, c192be9a

## User preferences

- when the user says "先完善功能流程，后续提供真实账号测试", build and mock-verify the complete production-shaped flow first, then defer real-account validation until credentials are available [Task 1][Task 3]
- when the user narrows scope to PC website Native QR payment with manual renewal, do not implicitly expand to JSAPI, H5, mini-program payment, automatic renewal, or other payment modes [Task 1][Task 3]
- when refund rules for consumed points or subscription entitlement reversal are undefined, do not add a refund path that returns money while leaving entitlements active; keep refund/reconciliation explicitly out of scope [Task 1][Task 2][Task 3]
- when implementing payment configuration, request non-sensitive identifiers and configuration status only; never ask for or retain private keys, certificates, licenses, or full `.env` contents [Task 1][Task 2]
- preserve unrelated dirty-worktree changes while implementing payment hardening; integrate with them rather than reverting them [Task 2][Task 3]
- when asked to add a payment page “参考这个文件”, connect the consumer UI to the existing catalog/order/QR/query APIs rather than replacing it with a static prototype; keep admin product configuration separate from checkout [Task 4]
- when the user says “提交，推送”, stage only the selected billing files, preserve unrelated edits, and complete the rebase/push flow [Task 4]

## Reusable knowledge

- Existing payment foundations include personal-account orders, Native/precreate flows, signed callbacks, webhook persistence, and transactional/idempotent entitlement granting; production readiness still requires active query/close, merchant identity checks, order idempotency, key rotation, and operational recovery [Task 1]
- Alipay signed calls are centralized in `src/lib/payments/providers.ts`; `alipay.trade.query` and `alipay.trade.close` verify provider references and normalize status/amount. Production requires `ALIPAY_APP_ID`, application private key, Alipay public key, `ALIPAY_SELLER_ID`, public HTTPS notify URL, and `ALIPAY_ENV=production`; seller identity is a fail-closed gate [Task 2]
- Payment order creation uses shared same-origin/JSON checks, UUID `x-idempotency-key`, account-scoped PostgreSQL advisory locking, metadata idempotency lookup, and an atomic `created -> pending` provider-call claim. Provider failure releases the claim for retry [Task 2][Task 3]
- `src/app/api/personal/payment-orders/sync/route.ts` actively queries either provider, validates provider order reference plus amount/currency and merchant identity before entitlement granting, and closes expired/provider-closed orders. The billing UI supports manual sync and terminal `paid`, `closed`, `failed`, and `refunded` states [Task 2][Task 3]
- WeChat V3 verification is serial-aware via `Wechatpay-Serial`, supports public-key/platform-certificate compatibility and overlapping rotation through `WECHATPAY_VERIFICATION_KEYS_JSON`; webhook checks include WeChat `appid` and `mchid` [Task 3]
- Deployment propagation for payment variables is covered by `.env.example`, `scripts/ops/render-prod-env.mjs`, and deployment docs; a temporary render check verified the generated Payments section retained notification URL, merchant ID, public-key ID, and public key [Task 3]
- The payment page already has `/personal/billing`, `/api/personal/catalog`, `/api/personal/payment-orders`, and `/api/personal/payment-orders/sync`; the billing-admin surface keeps those contracts and uses `personal-product-management-panel.tsx`, `personal-product-create-dialog.tsx`, `personal-product-preview.tsx`, and `src/app/admin/admin-page.css` [Task 4]
- Billing admin tabs are `基础配置`, `权益说明`, and `前端展示`. Recharge products expose code, name, price, Seedance rate, integer points, validity, status, and sort order; rate and points calculate bidirectionally. The tested example was 10 points / rate 1.5, then 20 points / rate 0.75, then rate 1.5 / 10 points [Task 4]
- The desktop preview defaults to a narrow collapsed rail with an accessible expand/collapse control; smaller layouts hide the preview content without breaking the editor grid. Focused recharge/billing tests (20), `pnpm ts-check`, `pnpm build`, and `git diff --check` passed before commit `c192be9a3900461f01b9c58f5dca2eaab249d0a3` (`feat: 更新计费基础配置与预览折叠`) was rebased and pushed with `HEAD...origin/main` at `0 0` [Task 4]
- Related skill: `skills/kele0707-payment-hardening/SKILL.md`
- Related skill: `skills/kele0707-git-sync/SKILL.md` for preserving dirty work, Chinese commit/rebase/push, and final `0 0` verification when payment changes later need delivery

## Failures and how to do differently

- Symptom: a QR precreate adapter and callback handler look complete. Cause: active status recovery, merchant checks, idempotency, refund, or reconciliation were not assessed. Fix: audit each production boundary separately before calling payment launch-ready [Task 1]
- Symptom: initial Alipay hardening missed seller identity, same-origin protection, direct `TRADE_CLOSED` handling, or server-side idempotency. Cause: relying on the happy-path callback flow. Fix: add focused contract tests for each control and verify production bypasses are fail-closed [Task 2]
- Symptom: WeChat sync route tests fail because only Alipay is dispatched, or TypeScript rejects WeChat-only fields. Cause: provider-specific branches and union narrowing were incomplete. Fix: dispatch by provider, use explicit `'appId' in providerOrder` / `'mchId' in providerOrder` guards, and rerun focused tests/typecheck [Task 3]
- Symptom: official web search returns HTTP 502. Cause: search path instability. Fix: use direct official documentation URLs with `curl.exe` or `Invoke-WebRequest` [Task 1]
- Symptom: temporary-directory cleanup is blocked by local PowerShell policy. Cause: compound recursive cleanup commands. Fix: use small auditable steps and `apply_patch` for known temporary files [Task 3]
- Symptom: the supplied local HTML prototype cannot open in the browser. Cause: `file://` navigation is blocked by browser policy. Fix: inspect the source through PowerShell; do not attempt policy workarounds [Task 4]
- Remaining boundary: refund APIs, refund queries, entitlement/points clawback, daily bill reconciliation, and real merchant end-to-end tests were not implemented; do not claim complete payment launch closure [Task 2][Task 3]

# Task Group: D:\hyli\kele-0707 episode-specific analyze_script diagnosis and batch selected-shot polishing
scope: Diagnose a named episode from task/API-step evidence, or implement and deliver the selected-shot custom-polish workflow without misrepresenting unverified browser or paid-task execution.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse the API/UI contract and diagnostic route for the current checkout; re-open the selected live task and re-verify the deployed build before attributing a current episode failure or claiming browser validation.

## Task 1: Diagnose episode 14 `analyze_script` failure from the live automated workflow, uncertain

### rollout_summary_files

- rollout_summaries/2026-08-05T02-39-38-ujLu-episode14_diagnosis_and_batch_shot_polish_push.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\05\rollout-2026-08-05T10-39-38-019fcfca-6e74-77a3-824d-ccf089d36b9c.jsonl, updated_at=2026-08-06T11:12:38+00:00, thread_id=019fcfca-6e74-77a3-824d-ccf089d36b9c, aggregate canvas evidence only; no task ID, API step, or raw error was recovered)

### keywords

- episode 14, analyze_script, automated workflow, workflow-runs, api-steps, task ID, parsed output, raw error, /api/workspaces/{workspaceId}/workflow-runs/{runId}/tasks/{taskId}/api-steps, <internal-host>:3000, 失败 4

## Task 2: Add and deliver batch selected-shot custom polishing, success

### rollout_summary_files

- rollout_summaries/2026-08-05T02-39-38-ujLu-episode14_diagnosis_and_batch_shot_polish_push.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\05\rollout-2026-08-05T10-39-38-019fcfca-6e74-77a3-824d-ccf089d36b9c.jsonl, updated_at=2026-08-06T11:12:38+00:00, thread_id=019fcfca-6e74-77a3-824d-ccf089d36b9c, implemented, validated, rebased, and pushed the batch polish feature)

### keywords

- batch selected-shot polishing, 批量操作, 重新润色, EpisodesView.jsx, EpisodesBoardLayout.jsx, /api/projects/[id]/shots/content-actions, manual_operation, polish_shots, polishPromptOverride, assertProjectCapabilities, storyboard_text, 200 shots, 2c72326022b51086a7554bc4a0e7aa38f6b9323b

## User preferences

- when the user requests a named episode's root cause, inspect the exact node/task and API-step evidence, including task ID, parsed output, and raw error; do not infer it from aggregate canvas status [Task 1]
- when the user explicitly requests `提交并推送` after implementation, complete the commit/rebase/push workflow after validation; use a Chinese commit message [Task 2]

## Reusable knowledge

- The formal detail route is `/api/workspaces/{workspaceId}/workflow-runs/{runId}/tasks/{taskId}/api-steps`; aggregate canvas `失败 4` is not episode-specific diagnosis evidence. [Task 1]
- Batch UI uses a shared single/batch polishing dialog in `EpisodesView.jsx`; `EpisodesBoardLayout.jsx` exposes `批量操作 -> 重新润色`. Explicitly selected shots must load their containing scenes before the dialog opens. [Task 2]
- `POST /api/projects/[id]/shots/content-actions` caps input at 200 shots and creates one exact-shot `manual_operation / polish_shots` task per target. It performs one `assertProjectCapabilities` call over all selected shots and one admission check; `polishPromptOverride` is request-scoped and canonical writes remain per-shot `storyboard_text` updates. [Task 2]
- Focused changed-area tests (20), `pnpm ts-check`, `pnpm build`, and `git diff --check` passed. Commit `2c72326022b51086a7554bc4a0e7aa38f6b9323b` (`增加批量小节自定义重新润色`) was rebased onto a remote 13 commits ahead and pushed; local and remote `main` matched with a clean worktree. [Task 2]

## Failures and how to do differently

- Symptom: browser canvas shows failures but the requested episode cannot be identified. Cause: stale/missing tabs and truncated DOM/image output never yielded its task ID or raw error. Fix: use the formal API-step read path or a diagnostic CLI first; do not claim a cause. [Task 1]
- Symptom: code validation passes but the browser cannot exercise the feature. Cause: port `3000` served an older remote build and local `3333` had no matching authenticated session. Fix: report browser visual verification and paid-task submission as incomplete; do not submit a credit-consuming polish task without explicit request. [Task 2]

# Task Group: D:\hyli\kele-0707 analyze_script screen-text contracts, source integrity, and runtime deployment
scope: Diagnose and fix structured screen-text failures from step-1 carrier classification through step-2 source recovery and final version injection; use for Worker-chain validation and deployment verification.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse for the current `screen_text_segment_v3` contract and its Worker call path; re-check the deployed Worker revision and selected live task before attributing a current failure to this historical incident.

## Task 1: Repair provisional screen-text carrier handling and deploy it, success

### rollout_summary_files

- rollout_summaries/2026-08-03T08-55-27-VwR1-analyze_script_screen_text_diagnosis_and_rebase_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\sessions\2026\08\03\rollout-2026-08-03T16-55-27-019fc6d5-c9a4-7562-bd5d-a4edf3052fbf.jsonl, updated_at=2026-08-03T10:31:02+00:00, thread_id=019fc6d5-c9a4-7562-bd5d-a4edf3052fbf, diagnosed the exact live failure, rebased the fix, and pushed it)
- rollout_summaries/2026-07-30T03-35-46-IIGS-screen_text_recognition_and_cross_language_shot_duration_des.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\30\rollout-2026-07-30T11-35-46-019fb117-ab4e-75b1-9ff6-2e1cbdfe7722.jsonl, updated_at=2026-08-04T02:45:55+00:00, thread_id=019fb117-ab4e-75b1-9ff6-2e1cbdfe7722, implemented the original v3 recognition contract and recorded the staged-validation boundary)

### keywords

- D:\hyli\kele-0707, analyze_script, screen_text_segment_v3, screenText.contractStage, type_split, carrier: null, lockStep2ScreenTextSegments, buildScreenTextDetectionMeta, SCREEN_TEXT_SEGMENT_CONTRACT_VIOLATION, carrier is required, analyze-script-structure-service.js, 12331640

## Task 2: Diagnose and repair screen-text source-integrity failures in episodes 3 and 10, partial

### rollout_summary_files

- rollout_summaries/2026-08-04T09-17-58-OzVk-analyze_script_screen_text_local_fix_and_runtime_restart.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\04\rollout-2026-08-04T17-17-58-019fcc10-c255-74d3-b4ab-61200600c03e.jsonl, updated_at=2026-08-04T10:50:30+00:00, thread_id=019fcc10-c255-74d3-b4ab-61200600c03e, local source-integrity fixes and full-stack restart verified; remote deployment/rerun blocked by SSH authorization)

### keywords

- D:\hyli\kele-0707, episodes 3 and 10, SCREEN_TEXT_SEGMENT_CONTRACT_VIOLATION, step 2 sourceText must exist in originalFormat, lockStep2ScreenTextSegments, injectVersionNamesToOriginalFormat, originalFormat, screenText.sourceText, <internal-host>, Permission denied (publickey,password), 6473a511, fad8d8ea, c047bd9f

## User preferences

- when the user asks about one episode's current `analyze_script` failure, re-open that latest node and report its task ID, API step, parsed output, and raw error instead of reusing an aggregate canvas diagnosis [Task 1]
- when the user asks to recognize on-screen text with an LLM and store it as a type, preserve it as structured screen text rather than degrading it to ordinary dialogue/subtitle strings [Task 1]
- when a retry would consume model credits, finish code and runtime validation first; do not trigger another LLM retry without an explicit request [Task 1]
- for named episode failures, begin read-only and inspect the latest task/node and API-step detail for each episode; do not rely on aggregate canvas/precheck status or trigger credit-consuming retries before code/runtime validation [Task 2]
- when implementation is approved for a risky narrow fix, preserve unrelated work with an isolated worktree and focused regression slices; use Chinese commit messages [Task 2]

## Reusable knowledge

- The direct live failure was post-provider: Volcengine `script_structure.step_1` succeeded, its `scene_object` screen text had `carrier: null`, and the Worker emitted `SCREEN_TEXT_SEGMENT_CONTRACT_VIOLATION: carrier is required: screen-text-d1d7924feed98875`. It was a local contract failure, not provider, billing, or scheduler failure. [Task 1]
- `screen_text_segment_v3` permits a provisional step-1 `scene_object` with `screenText.contractStage='type_split'` and `carrier: null`; `lockStep2ScreenTextSegments` must supply real `sourceText` and a nonempty carrier, remove the provisional marker, and final `buildScreenTextDetectionMeta` remains strict. Historical completed scripts are not silently rewritten; old checkpoints rerun from step 1. [Task 1]
- Trace the full chain: `analyze_script` -> `services/worker/structure/analyze-script-structure-service.js` -> `src/lib/script-analysis/screen-text-segment.ts` -> canonical `episodes.structured_script` readback. A first attempt that only relaxed normalization missed the separate post-`step1TypeSplit` `validateScreenTextSegments(step1Result)` call. [Task 1]
- The deployed rebased result retained upstream source-text carrier recovery plus local v3 semantics. Focused tests passed `15/15`, `pnpm ts-check` passed after `pnpm install --frozen-lockfile`, `git diff --check` passed, and commit `12331640` was pushed. [Task 1]
- For implicit screen text, `lockStep2ScreenTextSegments` may fall back only to nonempty Step-1 `screenText.sourceText` independently proven to be an `originalFormat` substring; it must never guess or concatenate replacement text. Explicit markers remain strict. [Task 2]
- `injectVersionNamesToOriginalFormat` must preserve screen-text `originalFormat`, `content`, and `screenText.sourceText` while normalizing `mentionedObjects` and `presentObjects` version metadata. The local implementation used regression tests in `tests/analyze-script-screen-text-segment.test.mjs` and `tests/analyze-script-version-injection-ownership.test.mjs`; focused checks `41/41`, `pnpm ts-check`, and `git diff --check` passed. [Task 2]

## Failures and how to do differently

- Symptom: a provisional-carrier patch passes helper tests but live retry still fails. Cause: a duplicated strict Worker validation remains after step 1. Fix: search the entire runtime call chain for validations and cover the service-level path, checkpoint recovery, final canonical validation, and the actual Worker process. [Task 1]
- Symptom: browser aggregate indicates several errors. Cause: it can include older failures or unrelated step-2 violations. Fix: select the exact latest task/node and inspect its raw step result before naming a cause. [Task 1]
- Symptom: typecheck reports missing declared modules such as `react-markdown`, `remark-gfm`, or `qrcode`. Cause: installed dependencies are absent, not necessarily a source regression. Fix: run `pnpm install --frozen-lockfile` before treating the errors as code failures. [Task 1]
- Symptom: Step 2 produces a label/summary rather than source text, or version injection mutates source text. Cause: its text no longer occurs contiguously in `originalFormat`. Fix: enforce the substring proof/fallback above and exempt screen-text source fields from version-name injection. [Task 2]
- Symptom: local repair succeeds but live rerun is requested. Cause: remote `dms@<internal-host>` SSH returned `Permission denied (publickey,password)`. Fix: do not claim deployment/rerun until authorized remote access is available; local `127.0.0.1` restart does not validate the remote host. [Task 2]

# Task Group: D:\hyli\kele-0707 split_shots first-scene diagnosis and multilingual source-language design
scope: Diagnose excessive `split_shots` subsection counts from authenticated runtime evidence and route the pending multilingual script/metrics design.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse the diagnosis evidence and pipeline map for similar live split-shot investigations; treat the multilingual data contract and formulas as pending design, not approved implementation.

## Task 1: Diagnose scene `1-1` excessive subsection splitting, partial

### rollout_summary_files

- rollout_summaries/2026-07-31T11-22-09-KeOx-first_scene_split_diagnosis_multilingual_shot_metrics.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\31\rollout-2026-07-31T19-22-09-019fb7e9-03a0-7391-a514-d772d408b605.jsonl, updated_at=2026-08-03T06:17:15+00:00, thread_id=019fb7e9-03a0-7391-a514-d772d408b605, confirmed 20 canonical subsections but did not prove the generating layer)

### keywords

- D:\hyli\kele-0707, project 55b5d59a-2c0b-4170-8847-685f452b41bc, scene 1-1, split_shots, initial-shots.ts, length.ts, script-display.ts, shotDurationCharacterCount, <=10, <40, task_start, canonical readback, <internal-host>:3000

## Task 2: Plan multilingual source-language detection and target-language speech duration for shot splitting, partial

### rollout_summary_files

- rollout_summaries/2026-07-31T11-22-09-KeOx-first_scene_split_diagnosis_multilingual_shot_metrics.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\31\rollout-2026-07-31T19-22-09-019fb7e9-03a0-7391-a514-d772d408b605.jsonl, updated_at=2026-08-03T06:17:15+00:00, thread_id=019fb7e9-03a0-7391-a514-d772d408b605, product/design baseline only; no user approval or implementation)
- rollout_summaries/2026-07-30T03-35-46-IIGS-screen_text_recognition_and_cross_language_shot_duration_des.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\30\rollout-2026-07-30T11-35-46-019fb117-ab4e-75b1-9ff6-2e1cbdfe7722.jsonl, updated_at=2026-08-04T02:45:55+00:00, thread_id=019fb117-ab4e-75b1-9ff6-2e1cbdfe7722, user selected target-language length as the real split input; audio-availability strategy remains undecided)

### keywords

- D:\hyli\kele-0707, sourceLanguage, dialogueLanguage, dialogueTranslations, translate_episode_dialogues, split_shots, languageMeta, textMetrics, wordCount, effectiveLength, shotDurationCharacterCount, estimatedSpeechSeconds, pre-generated TTS, hybrid validation, atomic dialogue sentences

## User preferences

- when the user asks why a visible scene is split into many subsections, diagnose first across raw text, frozen `split_shots` input, provider output, persistence, and UI; do not make code changes or assert a root cause before those layers agree [Task 1]
- when planning multilingual support, the user selected per-episode automatic source-language detection with manual override and requested a detailed plan; keep input source language separate from the `dialogueLanguage` generation target, and wait for implementation approval [Task 2]
- for market versions, treat target-language length as the actual shot-splitting input, not an overlength warning; allow different scene-local boundaries while preserving atomic dialogue sentences, action continuity groups, and enter/exit groups [Task 2]

## Reusable knowledge

- Signed-in Chrome proved scene `1-1` contained 20 real canonical/tree subsection nodes, not merely duplicate rendering. `split_shots` is compound: candidate split -> `assign_off_screen_status` -> `polish_shots` -> canonical writeback -> canonical readback. [Task 1]
- `initial-shots.ts` accumulates to `shotDurationCharacterCount`, merges at `<=10` effective units, and redistributes below `40`; `length.ts` prefers `segment.wordCount` but otherwise uses Chinese-oriented fallbacks; `script-display.ts` computes effective length and duration. These are plausible contributors, not the proven cause of this incident. [Task 1]
- `dialogueLanguage` supports `zh-CN`/`en` and controls generated dialogue translation before `split_shots`; it is not source-language truth. Existing prompts and metrics have Chinese assumptions including 25-character dialogue splitting and `screenText.sourceLanguage: 'zh-CN'`. [Task 2]
- The unapproved proposal stores settings and `languageMeta`/versioned `textMetrics` in existing JSON fields, freezes language/metric versions in task input, makes prompts language-aware while code owns counts/content conservation, and requires normal `analyze_script -> split_shots` reruns instead of silent historical backfill. [Task 2]
- Current `dialogueTranslations` cover `speech`, `legacy_quote`, and `screen_text`, but English translation is currently applied after split-shot generation, so it cannot change shot boundaries. Estimate spoken seconds by language, punctuation, numbers, and speaking style until real TTS duration can calibrate it. [Task 2]

## Failures and how to do differently

- Symptom: 20 visible subsections invite an immediate model or UI diagnosis. Cause: the rollout never acquired the exact task ID, `task_start` snapshot, provider payload/output, or canonical DB readback. Fix: obtain those layers before claiming root cause; the authenticated remote browser is decisive when local data targets another host. [Task 1]
- Symptom: implementation starts from the design fields or formulas. Cause: the multilingual plan was not approved or validated. Fix: retain it as a design baseline, reread the relevant files with explicit UTF-8 paths, preserve pre-existing dirty files, and seek approval before editing. [Task 2]
- Symptom: translated text length is measured with raw characters and ignored by `split_shots`. Cause: the current Chinese-oriented metric and translation order are not language-comparable. Fix: make the chosen target-language spoken-duration metric an input before split grouping; do not implement until the user chooses estimation-only, pre-generated TTS, or hybrid validation. [Task 2]

# Task Group: D:\hyli\kele-0707 focused Git rebase and push workflow
scope: Rebase and push a focused Kele-0707 fix after HTTP Git Credential Manager recovery while preserving unrelated work and verification evidence.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Use for explicit commit-and-push work on this checkout's `main`; use the existing skill for pull-only versus dirty-worktree distinctions and re-check remote/auth state live.

## Task 1: Rebase and push the screen-text carrier fix, success

### rollout_summary_files

- rollout_summaries/2026-08-03T08-55-27-VwR1-analyze_script_screen_text_diagnosis_and_rebase_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\sessions\2026\08\03\rollout-2026-08-03T16-55-27-019fc6d5-c9a4-7562-bd5d-a4edf3052fbf.jsonl, updated_at=2026-08-03T10:31:02+00:00, thread_id=019fc6d5-c9a4-7562-bd5d-a4edf3052fbf, recovered stale HTTP auth, resolved rebase conflicts, and confirmed pushed clean main)
- rollout_summaries/2026-07-30T03-35-46-IIGS-screen_text_recognition_and_cross_language_shot_duration_des.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\30\rollout-2026-07-30T11-35-46-019fb117-ab4e-75b1-9ff6-2e1cbdfe7722.jsonl, updated_at=2026-08-04T02:45:55+00:00, thread_id=019fb117-ab4e-75b1-9ff6-2e1cbdfe7722, initial local Chinese commit and stale-GCM failure; later rollout provides the successful push evidence)

### keywords

- D:\hyli\kele-0707, git credential reject, credential.interactive=never, <internal-host>:3001/quyuan/kele.git, git fetch origin main --prune, git rebase origin/main, pnpm install --frozen-lockfile, 12331640, HEAD...origin/main, 0 0, skills/kele0707-git-sync/SKILL.md

## User preferences

- when the user asks to pull latest, commit locally, and push, stage only the intended files, use a Chinese commit message, preserve unrelated work, perform an ordinary rebase rather than a force-push, and verify final `0 0` divergence [Task 1]

## Reusable knowledge

- The working remote is `http://<internal-host>:3001/quyuan/kele.git`. After stale HTTP authentication, `git credential reject` for `protocol=http`, `host=<internal-host>:3001` followed by `git -c credential.interactive=never fetch origin main --prune` succeeded. [Task 1]
- With remote 80 commits ahead, rebase the existing focused commit instead of creating a duplicate. Conflicts in screen-text code/tests and paired audit docs must retain both upstream carrier recovery and local v3 provisional-carrier semantics. [Task 1]
- Related skill: skills/kele0707-git-sync/SKILL.md. The successful finish was focused tests, `pnpm ts-check`, `git diff --check`, noninteractive push, final fetch, `HEAD...origin/main` `0 0`, and a clean worktree. [Task 1]

## Failures and how to do differently

- Symptom: a previous fetch fails and cached `origin/main` shows a seemingly clean divergence. Cause: cached remote state is not fresh evidence. Fix: only use branch counts/SHA after a successful authenticated fetch. [Task 1]
- Symptom: a local fix is already committed before sync. Cause: re-staging it risks an empty or duplicate commit. Fix: rebase the existing commit, resolve conflicts semantically, retest, then push. [Task 1]

# Task Group: D:\hyli\kele-0707 analyze_script / extract_objects pipeline audit and correctness risks
scope: Explain structured-analysis text/provenance changes and audit `extract_objects` completion semantics, reducer/commit behavior, and stale-ref risk in `D:\hyli\kele-0707`.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse for this checkout's current `analyze_script`, `extract_objects`, and workflow-task worker pipeline when the user wants the persisted analysis chain or a correctness audit; re-check prompt files, worker handlers, DB evidence, and completion semantics if code or project data changed.

## Task 1: Explain 6-2-2 subject loss and unresolved pronoun in analyzed script output, uncertain

### rollout_summary_files

- rollout_summaries/2026-07-13T03-57-03-uj6D-extract_objects_analysis_pipeline_risk_audit.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-13T11-57-12-019f599f-0c57-7e22-b28d-f1431048a917.jsonl, updated_at=2026-07-14T02:25:43+00:00, thread_id=019f599f-0c57-7e22-b28d-f1431048a917, static trace of why analyzed text dropped the explicit subject and did not resolve the pronoun)

### keywords

- D:\hyli\kele-0707, 6-2-2, analyze_script, pronoun resolution, postProcessSegments, analyze-script-structure-11-system-prompt.prompt.md, structured_script, version injection

## Task 2: Audit `extract_objects` reducer, atomic commit, and completion semantics, partial

### rollout_summary_files

- rollout_summaries/2026-07-13T03-57-03-uj6D-extract_objects_analysis_pipeline_risk_audit.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-13T11-57-12-019f599f-0c57-7e22-b28d-f1431048a917.jsonl, updated_at=2026-07-14T02:25:43+00:00, thread_id=019f599f-0c57-7e22-b28d-f1431048a917, traced the separated worker, reducer/commit path, and task-success boundary without applying a patch)

### keywords

- D:\hyli\kele-0707, extract_objects, extract_global_settings, object-extraction-reducer.ts, object-extraction-atomic-commit.ts, workflow-task-worker-extract-handlers.ts, default-task-executors.ts, SERIALIZABLE, writeTaskSuccess

## Task 3: Identify durable correctness risks in object extraction and task completion, partial

### rollout_summary_files

- rollout_summaries/2026-07-13T03-57-03-uj6D-extract_objects_analysis_pipeline_risk_audit.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-13T11-57-12-019f599f-0c57-7e22-b28d-f1431048a917.jsonl, updated_at=2026-07-14T02:25:43+00:00, thread_id=019f599f-0c57-7e22-b28d-f1431048a917, ranked code-level stale-ref and false-success risks as review findings)

### keywords

- D:\hyli\kele-0707, duplicate add, deduplicated ledger entry, stale refs, character_objects, location_objects, other_objects, lockWorkflowRunForWorkerFactWrite, FOR KEY SHARE, completion truth

## Task 5: Optimize step-8 presentObjects prompt, runtime envelope, and contract tests, success

### rollout_summary_files

- rollout_summaries/2026-07-22T07-51-53-Ag1U-present_objects_prompt_optimization_git_sync_local_stack_res.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\22\rollout-2026-07-22T15-51-58-019f88cf-4962-7a33-8909-13eda5368f46.jsonl, updated_at=2026-07-27T03:33:20+00:00, thread_id=019f88cf-4962-7a33-8909-13eda5368f46, aligned the prompt, runtime envelope, and strict contract without changing candidate scope, completion truth, or retry semantics)

### keywords

- D:\hyli\kele-0707, analyze_script, step 8, presentObjects, explicitMarkerStateBeforeSegment, normalizeIndexedPresentObjectsResponse, jsonMode, maxTokens:65536, analyze-script-structure-15-system-prompt.prompt.md, present-objects-contract.test.mjs

## Task 6: Count screen-visible characters as present in step-8 `presentObjects`, success

### rollout_summary_files

- rollout_summaries/2026-07-29T07-37-11-YPB3-screen_visible_character_presence_presentobjects.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\29\rollout-2026-07-29T15-37-11-019facce-5705-7b91-93dd-acee5c75ff65.jsonl, updated_at=2026-07-29T08:45:27+00:00, thread_id=019facce-5705-7b91-93dd-acee5c75ff65, added the explicit moving-screen-imagery rule, focused regression coverage, Chinese commit, and clean push)

### keywords

- D:\hyli\kele-0707, analyze_script, step 8, presentObjects, screen-visible-character, television, monitor, live stream, surveillance feed, video call, recording, voice-only, static photo, analyze-script-present-objects-prompt.test.mjs, cb002128, 23/23

## User preferences

- when the user asked `为什么把沈以去掉了？递给他中的他代称也没有转换成具体的人`, they wanted the explanation tied to the real analysis chain and persisted transformations, not a generic UI answer [Task 1]
- when the user’s issue pointed at `analyze_script` / `extract_objects`, the investigation had to follow the full worker/service/DB chain rather than answering only from displayed state [Task 1][Task 2]
- when the user asked to add a `道具持久性原则` and accepted the remaining proposed optimizations, preserve the existing candidate pool and completion truth, then implement the approved prompt/input/JSON-mode changes without re-asking the same product decisions [Task 5]

- when the user says a character is visible on a screen, keep the approved semantic boundary: moving television, phone, monitor, live-stream, surveillance, video-call, or recording imagery counts in `presentObjects.characters`; voice-only participants and static/frozen imagery do not [Task 6]

## Reusable knowledge

- Pronoun and ambiguous-reference resolution for this bug family is specified in `services/provider-gateway/prompts/legacy-project-analysis/analyze-script-structure-11-system-prompt.prompt.md`, while `postProcessSegments()` in `services/worker/structure/analyze-script-structure-service.js` is the main post-processing normalization boundary after LLM output [Task 1]
- Later version suffixes such as `[初始]` are injected by normalization/version-injection steps, so a versioned object label in final text is not by itself evidence that the subject disappeared there [Task 1]
- In this checkout, separated-worker `extract_global_settings` and `extract_objects` call `services/worker/structure/analyze-episodes-characters-service.js`, `runExtractObjects()` in `src/lib/workflow/default-task-executors.ts` still waits on postconditions, and the newer reducer/commit path is `object-extraction-reducer.ts` plus `object-extraction-atomic-commit.ts` under `SERIALIZABLE` transactions [Task 2]
- The focused regression suite that already passed for this area was `node --experimental-strip-types --loader ./tests/support/ts-extension-loader.mjs --test tests/object-extraction-atomic-reducer.test.mjs tests/object-extraction-atomic-commit.test.mjs`; reuse it before or after any reducer/commit patch [Task 2]
- The highest-signal review findings were that duplicate `add` actions currently become `deduplicated` ledger entries without re-adding the reused object to episode refs, and absent-category writes can leave stale `character_objects` / `location_objects` / `other_objects` values in place because per-column write flags do not clear them automatically [Task 2][Task 3]
- `writeTaskSuccess()` in `services/worker/workflow-task-worker-writes.ts` runs only after handler-level canonical work, so completion semantics for this area are split across reducer, commit, handler, worker success write, and postcondition checks rather than enforced in one place [Task 2][Task 3]
- For step 8, prompt rules must keep each `index` represented once, use canonical candidate names, and return complete `characters` / `locations` / `others` arrays (empty arrays for non-body text). Props remain after use, later non-mention, or visible placement; remove only after explicit discard, transfer out of frame, out-of-sight concealment, destruction/disappearance, loss elsewhere, or an uncarried scene boundary. A local phone holder can remain present, but a remote voice cannot; `speechType` alone is insufficient [Task 5]
- The prompt surface is `services/provider-gateway/prompts/legacy-project-analysis/analyze-script-structure-15-system-prompt.prompt.md`; runtime assembly is `services/worker/structure/analyze-script-structure-service.js`. The validated envelope keeps full `content` / `originalFormat`, renames `previousPresentObjects` to `explicitMarkerStateBeforeSegment`, and explicitly uses `jsonMode:true,maxTokens:65536` [Task 5]
- Focused contract coverage is `tests/analyze-script-present-objects-prompt.test.mjs`, `tests/analyze-script-present-objects-version-input.test.mjs`, and `tests/analyze-script-present-objects-contract.test.mjs`; this run passed 22/22 plus `node --check`, `pnpm ts-check`, and `git diff --check` [Task 5]
- Screen-visible moving imagery in a television, phone, monitor, live stream, surveillance feed, video call, or recording counts in `presentObjects.characters`; voice-only remote participants and static photos, avatars, archive photos, or freeze frames do not. The prompt surface remains `services/provider-gateway/prompts/legacy-project-analysis/analyze-script-structure-15-system-prompt.prompt.md`, with runtime assembly in `services/worker/structure/analyze-script-structure-service.js` [Task 6]
- Add the narrow prompt-contract regression before the prompt rule. The validated screen-visible change passed 23/23 focused checks, `pnpm ts-check`, and `git diff --check` without altering candidate scope, runtime envelope, retry policy, persistence, postconditions, or completion truth; existing structured results require an `analyze_script` rerun [Task 6]

## Failures and how to do differently

- Symptom: a user asks why analyzed text dropped a concrete subject or failed to resolve a pronoun. Cause: the visible UI output is only the end of a longer prompt -> postprocess -> normalization chain. Fix: trace the persisted analysis pipeline first, starting from the prompt contract and `postProcessSegments()`, before speculating about the frontend [Task 1]
- Symptom: the local checkout cannot prove the exact user episode or project row. Cause: the local DB query returned no rows for that project, so the rollout had only static code evidence. Fix: preserve the explanation as a code-path audit and label runtime conclusions as uncertain until live project data is available [Task 1][Task 2]
- Symptom: `extract_objects` looks successful while semantic correctness is still wrong. Cause: reducer dedup behavior, stale-ref preservation, and split completion boundaries can all mask a partial or misleading result. Fix: treat these as review findings until a regression test or patch confirms them, and verify both canonical object refs and task-success/postcondition behavior after changes [Task 2][Task 3]
- Symptom: expanded parity/replay suites fail after a step-8 prompt contract change. Cause: their fixtures or anchors can be stale or unrelated to the focused change. Fix: report the passing focused contract checks separately; do not claim the full suite passed or roll back a validated change solely on unrelated baseline failures [Task 5]
- Symptom: a screen-visible regression assertion fails after the prompt rule is added. Cause: contract wording differs from the prompt text. Fix: align prompt and assertion exactly rather than weakening the test [Task 6]

# Task Group: D:\hyli\kele-0707 split_scenes boundary debugging, runtime migration recovery, and git sync
scope: Diagnose `split_scenes` boundary failures in `D:\hyli\kele-0707`, recover the local workflow runtime when schema drift breaks it, and finish the resulting Chinese commit through fetch/rebase/push.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse for this checkout's current `split_scenes`, local workflow-runtime restart, and `main` sync path when the user points at exact failed workflow nodes or asks to recover and ship the fix; re-check browser-visible node details, migrations, and remote state if the code or schema moved.

## Task 1: Diagnose episode 64 `split_scenes` boundary omission from live node evidence, success

### rollout_summary_files

- rollout_summaries/2026-07-14T06-09-27-Cp4Z-split_scenes_migrations_and_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-14T14-09-32-019f5f3e-9ebb-7db3-8aca-723d3e327dc9.jsonl, updated_at=2026-07-14T08:35:55+00:00, thread_id=019f5f3e-9ebb-7db3-8aca-723d3e327dc9, proved the failure was a missing-tail-segment omission in provider output rather than a parser bug)

### keywords

- D:\hyli\kele-0707, split_scenes, segmentIndices, 91/92, auto-scene-split-structure.ts, auto-scene-split-boundary-provider.ts, non-body tail, doubao-seed-2-0-pro-260215, detail modal

## Task 2: Recover the local workflow runtime by applying pending migrations, success

### rollout_summary_files

- rollout_summaries/2026-07-14T06-09-27-Cp4Z-split_scenes_migrations_and_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-14T14-09-32-019f5f3e-9ebb-7db3-8aca-723d3e327dc9.jsonl, updated_at=2026-07-14T08:35:55+00:00, thread_id=019f5f3e-9ebb-7db3-8aca-723d3e327dc9, restored the broken local stack by applying 8 pending migrations and rechecking runtime health)

### keywords

- D:\hyli\kele-0707, workflow_task_operator_is_authorized, apply-db-migrations.sh, schema_migrations, ENV_FILE=/dev/null, MIGRATIONS_DIR, 3333, 8091, 8224, start-dev-with-local-bin.sh

## Task 3: Rebase the Chinese local fix onto latest `origin/main` and push, success

### rollout_summary_files

- rollout_summaries/2026-07-14T06-09-27-Cp4Z-split_scenes_migrations_and_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-14T14-09-32-019f5f3e-9ebb-7db3-8aca-723d3e327dc9.jsonl, updated_at=2026-07-14T08:35:55+00:00, thread_id=019f5f3e-9ebb-7db3-8aca-723d3e327dc9, rebased one Chinese commit across a docs conflict, reran focused checks, and pushed `main`)

### keywords

- D:\hyli\kele-0707, git -c credential.interactive=never, git fetch origin main, git rebase origin/main, docs/audit/workflow-active-chains-cn.md, 54e11968, git rev-list --left-right --count, 0 0

## User preferences

- when the user reports an exact failed workflow node like episode `64` and says to find the cause, inspect the exact failing boundary result instead of guessing from the generic error string [Task 1]
- when the user authorizes schema changes and says to restart fully, finish the runtime recovery end-to-end and verify the full stack rather than stopping at a DB-only fix [Task 2]
- when the user asks to pull latest, keep the local commit in Chinese, and push, preserve the existing Chinese commit and carry the sync through rebase/push instead of inventing a new commit or stopping at local verification [Task 3]

## Reusable knowledge

- `src/lib/auto-scene-split-structure.ts` is the hard validator for this bug family: `segmentIndices` must be contiguous, ordered, and cover `0..totalSegments-1`; the observed failure was coverage `91/92`, not a malformed scene schema [Task 1]
- `services/provider-gateway/auto-scene-split-boundary-provider.ts` retries invalid boundaries up to 3 times and only then refuses the write, so the node-detail modal is a high-value place to confirm the exact missing range before editing code [Task 1]
- In the validated failure, the provider returned two scenes covering `0..28` and `29..90`, while the prompt input still contained the trailing non-body segment at index `91`; this is the signature for the missing-tail-segment bug [Task 1]
- The local runtime recovery path that worked was `ENV_FILE=/dev/null MIGRATIONS_DIR=/d/hyli/kele-0707/migrations bash ./scripts/ops/apply-db-migrations.sh`, then `.runtime/start-dev-with-local-bin.sh`, then health checks on `http://127.0.0.1:3333/api/health`, `http://127.0.0.1:8091/healthz`, and `http://127.0.0.1:8224/varz` plus worker/scheduler log tails [Task 2]
- The concrete schema symptom was missing PostgreSQL function `public.workflow_task_operator_is_authorized(uuid)`; after the 8 pending migrations, `schema_migrations` pending count reached `0` and the workflow worker stopped exiting with `UndefinedFunction` [Task 2]
- The dependable push path here was fully non-interactive: `git -c credential.interactive=never fetch origin main` -> `git rebase origin/main` -> focused tests -> `git -c credential.interactive=never push origin main`, followed by `git rev-list --left-right --count main...origin/main` expecting `0 0` [Task 3]

## Failures and how to do differently

- Symptom: browser scraping stalls on a heavy workflow page. Cause: `domSnapshot()` can time out before the useful payload is visible. Fix: pivot to the workflow inspector detail modal and API-call tabs, which exposed the exact `91/92` gap directly [Task 1]
- Symptom: a restart keeps failing even though processes relaunch. Cause: the underlying DB schema is behind, not the process supervisor. Fix: stop the broken stack, run the pending migrations, verify the missing function exists, then restart and recheck runtime health [Task 2]
- Symptom: interactive Git commands hang during sync or push. Cause: this repo's HTTP remote and credential manager can block automation with prompts. Fix: use non-interactive Git settings, resolve the one markdown conflict carefully, and never force-push just to bypass a docs-only rebase conflict [Task 3]

# Task Group: D:\hyli\kele-0707 script-analysis / split-shots enter-exit audit and persisted marker debugging
scope: Diagnose version or enter/exit marker inconsistencies that are already persisted in split-shot text, audit the full generation-to-display chain, and carry fixes through rebase/push in `D:\hyli\kele-0707`.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse for this checkout's current script-analysis, split-shot, and shot-display enter/exit pipeline; re-check DB rows, provider-gateway inputs, worker code, and upstream remote changes if the checkout or schema moved.

## Task 1: Diagnose persisted `<澶у垬鐧诲満>` missing-version marker in split-shot output, partial

### rollout_summary_files

- rollout_summaries/2026-07-08T06-42-55-WYou-kele0707_diagnostic_sync_and_dev_restart.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-08T14-43-02-019f4077-1d2f-7bf0-a301-39788878b0ae.jsonl, updated_at=2026-07-10T06:44:59+00:00, thread_id=019f4077-1d2f-7bf0-a301-39788878b0ae, proved the missing version marker was already persisted in DB split-shot text rather than added by the frontend)

### keywords

- D:\hyli\kele-0707, 澶у垬鐧诲満, 86e751a0-ee0d-4b96-9d4f-55c57b0257aa, openingInfo, mergedEnterExitMarkers, structured_segments, split_shots, character_versions, Node pg, psql not found

## Task 2: Review and harden enter-exit consistency across script analysis and shot splitting, success

### rollout_summary_files

- rollout_summaries/2026-07-10T06-45-12-swiR-enter_exit_loophole_review_rebase_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-10T14-45-12-019f4ac5-ea63-7ac0-8eab-0b355ea4e6e1.jsonl, updated_at=2026-07-10T11:14:31+00:00, thread_id=019f4ac5-ea63-7ac0-8eab-0b355ea4e6e1, traced and tightened the enter/exit pipeline across analysis, split-shot resume input, and display helpers)

### keywords

- D:\hyli\kele-0707, enter-exit, presentObjects, analyze-script-structure-service.js, enter-exit.ts, auto-scene-enter-exit-markers.ts, shot-enter-exit-display.ts, split-shots-provider.ts, initialPresentObjects, structure-runtime-envelope-parity

## Task 3: Rebase, validate, and push the enter-exit fixes after remote drift, success

### rollout_summary_files

- rollout_summaries/2026-07-10T06-45-12-swiR-enter_exit_loophole_review_rebase_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-10T14-45-12-019f4ac5-ea63-7ac0-8eab-0b355ea4e6e1.jsonl, updated_at=2026-07-10T11:14:31+00:00, thread_id=019f4ac5-ea63-7ac0-8eab-0b355ea4e6e1, merged conflicts with advancing `origin/main`, reran focused checks, and pushed `main`)

### keywords

- D:\hyli\kele-0707, git fetch origin, git rebase origin/main, merge conflict, core.editor, cmd.exe /C exit 0, git diff --check, pnpm ts-check, 2fb668f7, 8547e882, git push origin main

## User preferences

- when the user points at a visible content/version mismatch and says `鎵鹃棶棰橈紝淇`, start from the real persisted data path and not from a frontend-only assumption [Task 1]
- when the user asks whether script-analysis enter/exit logic `鏈夋紡娲?`, treat it as a code-review / logic-audit request and trace the full chain instead of answering from one file [Task 2]
- when the user interrupts a debugging run to say `鎷夊幓鏈€鏂扮増`, they expect sync and later resumption without losing the diagnosis context [Task 1]

## Reusable knowledge

- For this bug family, prove first whether the wrong marker already lives in persisted split-shot data: in the validated case, `script_text`, `storyboard_text`, and `structured_segments[0].originalFormat` all already contained `<澶у垬鐧诲満>`, so the frontend read path was not the source [Task 1]
- The validated DB clue was that `structured_segments[0].mergedEnterExitMarkers` contained `{ type: '鐧诲満', character: '澶у垬', originalFormat: '<澶у垬鐧诲満>' }` while `character_versions` confirmed `澶у垬` has version `甯告湇`; this narrowed the likely fix area to shot-splitting / enter-exit marker merge logic [Task 1]
- The traced implementation chain for current enter/exit behavior is structured analysis -> `presentObjects` -> `insertObjectEnterMarkers()` -> `splitShotsFromStructuredScript()` -> `finalizeShotDisplayFields()` / `mergeEnterExitMarkersIntoNarrativeSegments()` [Task 2]
- `src/lib/script-analysis/enter-exit.ts` holds the older analysis-side first/last-appearance logic, while `src/lib/auto-scene-enter-exit-markers.ts` and `src/lib/shot-enter-exit-display.ts` are the key display-side marker insertion / merge helpers for split-shot and readback behavior [Task 2]
- `services/worker/structure/analyze-script-structure-service.js` now relies on `getStructuredScriptPresentObjectsCompletion`, `normalizeIndexedPresentObjectsResponse`, and `normalizePresentObjectsCheckpointSegments`, and `services/provider-gateway/split-shots-provider.ts` now threads `initialPresentObjects` from previous analyzed scenes when available [Task 2]
- The high-signal regression set for this area was `tests/analyze-script-version-injection-ownership.test.mjs`, `tests/shot-splitting-presence-display.test.mjs`, `tests/shot-splitting-enter-exit-marker.test.mjs`, and `tests/structure-runtime-envelope-parity.test.mjs`, plus `git diff --check` and `pnpm ts-check` before push [Task 2][Task 3]
- The rebase resolution that held preserved both local enter/exit fixes and remote changes: keep `split-shots-provider.ts` resume-contract input plus local `initialPresentObjects`, keep analysis-side presentObjects completion while merging remote checkpoint write behavior, and preserve both local and remote audit-doc notes when docs conflict [Task 3]

## Failures and how to do differently

- Symptom: a missing version marker looks like a UI formatting problem. Cause: the bad marker may already be persisted in `script_text` / `storyboard_text` / `structured_segments`. Fix: query the exact episode-scene-shot row first and inspect `mergedEnterExitMarkers` before patching read-side display code [Task 1]
- Symptom: direct DB inspection stalls because `psql` is unavailable on this machine. Cause: this checkout does not have `psql` on PATH. Fix: go straight to a Node `pg` query using the local `.env.local` connection details [Task 1]
- Symptom: a first rebase succeeds but `git push origin main` is rejected again. Cause: `origin/main` advanced during validation. Fix: `git fetch origin` -> `git rebase origin/main` -> rerun focused checks -> push, instead of retrying the stale push [Task 3]
- Symptom: scripted conflict rewriting fails on compressed JS with non-ASCII content. Cause: exact-string replacement is brittle under PowerShell quoting / encoding. Fix: anchor the edit on nearby ASCII code and rewrite only the needed slice without touching Chinese text [Task 2][Task 3]

# Task Group: D:\hyli\kele-0707 automated-workflow debugging, write-path validation, and browser-backed evidence gathering
scope: Debug `automated-workflow` mismatches in `D:\hyli\kele-0707`, especially when the user wants the real structured-data source, browser-visible proof, or a narrow `analyze_script` / `split_shots` fix carried through validation and push.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse for this checkout's current `automated-workflow` shot editor, `analyze_script`, `split_shots`, and browser-backed debugging flow; re-check the active provider-gateway, browser-extension state, remote host, and canonical shot fields if the checkout or tooling changes.

## Task 1: Diagnose unrelated `14-2-1` object enter markers from `presentObjects`, success

### rollout_summary_files

- rollout_summaries/2026-07-09T03-46-03-FRT9-14_2_1_presentobjects_enter_marker_diagnosis.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-09T11-46-12-019f44fb-8ba5-70e1-b751-cf3d2ca1b607.jsonl, updated_at=2026-07-09T03:57:06+00:00, thread_id=019f44fb-8ba5-70e1-b751-cf3d2ca1b607, traced the visible `14-2-1` marker noise back to structured-segment `presentObjects` and recovered Chrome-backed evidence gathering)

### keywords

- D:\hyli\kele-0707, presentObjects, 14-2-1, split_shots, insertObjectEnterMarkers, structuredSegments, auto-scene-enter-exit-markers.ts, contenteditable, Chrome login state, Codex Chrome Extension, Browser is not available: extension

## Task 4: Hide standalone exit markers in shot content and sync it to main, success

### rollout_summary_files

- rollout_summaries/2026-07-10T10-14-01-am44-hide_shot_exit_markers_display_and_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-10T18-14-01-019f4b85-193b-7902-917d-88585adcf774.jsonl, updated_at=2026-07-14T07:28:41+00:00, thread_id=019f4b85-193b-7902-917d-88585adcf774, display-only fix for standalone exit-marker rows plus Chinese local commit and push after auth/rebase recovery)

### keywords

- D:\hyli\kele-0707, ManualVideoPromptPanel, StructuredScriptPanel, ShotCard, shotExitMarkerDisplay.js, hideStandaloneExitMarkers, standalone exit marker, tests/shot-exit-marker-display.test.mjs, git credential reject, browser login page

## Task 5: Diagnose `45-3` split-shot empty-display failure and add the guardrails, success

### rollout_summary_files

- rollout_summaries/2026-07-14T06-37-53-JytF-45_3_split_shots_empty_display_and_non_body_scene_fix.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-14T14-37-58-019f5f58-a7ab-7420-a8f2-b73122fef5bf.jsonl, updated_at=2026-07-14T07:20:43+00:00, thread_id=019f5f58-a7ab-7420-a8f2-b73122fef5bf, traced a persisted `45-3` task failure to marker-only provider output and fixed both split-shot and pure-non-body-scene handling)

### keywords

- D:\hyli\kele-0707, 45-3, split_shots, contentPreview, <<<小节1>>>, contentLength: 9, empty scriptText, auto-scene-split-structure.ts, shot-splitting-presence-display.test.mjs, non-body scene

## Task 6: Diagnose `50-4-2` UI/text version mismatch and harden `analyze_script` version ownership, success

### rollout_summary_files

- rollout_summaries/2026-07-16T02-55-38-xT95-analyze_script_version_ownership_fix_commit_rebase_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\sessions\2026\07\16\rollout-2026-07-16T10-55-43-019f68d9-e51d-76f3-8097-526b12189f47.jsonl, updated_at=2026-07-16T09:19:15+00:00, thread_id=019f68d9-e51d-76f3-8097-526b12189f47, traced a browser-visible version-label mismatch to step-7 ownership drift and fixed it with validation plus marker rewrite)

### keywords

- D:\hyli\kele-0707, 50-4-2, analyze_script, step 7, version ownership, structured_script, injectVersionNamesToOriginalFormat, markerVersion, canonicalVersion, analyze-script-step7-version-validation.js, <internal-host>:3000

## Task 7: Diagnose `12-1-1` script mismatch and harden `split_shots` off-screen integrity, success

### rollout_summary_files

- rollout_summaries/2026-07-16T09-13-10-Qnz0-split_shots_off_screen_integrity_and_chinese_commit_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\sessions\2026\07\16\rollout-2026-07-16T17-13-16-019f6a33-8c25-7f71-84b4-dad6788644b6.jsonl, updated_at=2026-07-16T10:33:55+00:00, thread_id=019f6a33-8c25-7f71-84b4-dad6788644b6, traced a persisted `12-1-1` text mismatch to loose off-screen-status output validation and added retryable integrity guards)

### keywords

- D:\hyli\kele-0707, 12-1-1, split_shots, off-screen-status, scene-shot-off-screen-status.ts, ASSIGN_OFF_SCREEN_STATUS_SCRIPT_INTEGRITY_VIOLATION, ASSIGN_OFF_SCREEN_STATUS_SHOT_IDENTITY_VIOLATION, workflow-task-worker-writes.ts, task-execution-result-utils.ts

## Task 8: Diagnose episodes `38/39` `split_shots` failure from live workflow evidence, partial

### rollout_summary_files

- rollout_summaries/2026-07-20T06-13-10-xn3C-kele_0707_split_shots_diagnosis_version_span_fix_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\20\rollout-2026-07-20T14-13-16-019f7e28-30ce-7963-8105-68d9265886e4.jsonl, updated_at=2026-07-21T08:51:45+00:00, thread_id=019f7e28-30ce-7963-8105-68d9265886e4, established browser-visible running/pending state but not the terminal root cause)

### keywords

- D:\hyli\kele-0707, split_shots_38_1, split_shots_39_1, 38-1, 39-1, 25%, serial dependency, project f9389eb5-5c1d-4da2-bdf3-841b2faf39a6, <internal-host>:3000, workflow-active-chains.md

## Task 9: Fix short-name injection into a protected long versioned object name, success

### rollout_summary_files

- rollout_summaries/2026-07-20T06-13-10-xn3C-kele_0707_split_shots_diagnosis_version_span_fix_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\20\rollout-2026-07-20T14-13-16-019f7e28-30ce-7963-8105-68d9265886e4.jsonl, updated_at=2026-07-21T08:51:45+00:00, thread_id=019f7e28-30ce-7963-8105-68d9265886e4, narrow future-analysis fix with regression, Chinese commit `d955dc0c`, and push)

### keywords

- D:\hyli\kele-0707, analyze_script, version injection, protected spans, offsetAfterLastToken, lastIndexOf, analyze-script-structure-object-version-utils.js, analyze-script-version-injection-ownership.test.mjs, originalFormat, actionContent, d955dc0c

## Task 10: Constrain `analyze_script` Step 7 to episode-bound character versions, success

### rollout_summary_files

- rollout_summaries/2026-07-22T09-59-03-QxgA-kele0707_version_mismatch_stale_split_shots_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\22\rollout-2026-07-22T17-59-08-019f8943-b2da-7080-8f69-4128f5976f48.jsonl, updated_at=2026-07-27T06:45:36+00:00, thread_id=019f8943-b2da-7080-8f69-4128f5976f48, browser-confirmed 9-1 version drift fixed with episode-binding candidate filtering and server validation)

### keywords

- D:\hyli\kele-0707, analyze_script, Step 7, versionIds, character_versions, frozen snapshot, version_not_allowed_for_episode, analyze-script-step7-version-validation.js, analyze-script-input-envelope.ts, 9-1

## Task 11: Diagnose stale `split_shots` frozen-input failure, success

### rollout_summary_files

- rollout_summaries/2026-07-22T09-59-03-QxgA-kele0707_version_mismatch_stale_split_shots_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\22\rollout-2026-07-22T17-59-08-019f8943-b2da-7080-8f69-4128f5976f48.jsonl, updated_at=2026-07-27T06:45:36+00:00, thread_id=019f8943-b2da-7080-8f69-4128f5976f48, high-confidence code-path diagnosis; remote snapshot row was not directly read)

### keywords

- D:\hyli\kele-0707, split_shots, 78862e88-5913-4ef7-a89d-c8d95fe3d5f7, task_start, task_start:split_shots_recovery_v1, input_snapshot, providerSubmitted=false, d6d57872, workflow-task-input-recovery.ts

## Task 12: Repair historical episode 7 character-version bindings, success

### rollout_summary_files

- rollout_summaries/2026-07-27T08-52-31-FLq3-episode_7_character_version_mismatch_fixed.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\27\rollout-2026-07-27T16-52-31-019fa2c6-982e-7080-a199-cb68ee6482a3.jsonl, updated_at=2026-07-27T09:08:58+00:00, thread_id=019fa2c6-982e-7080-a199-cb68ee6482a3, production data repaired and API/UI comparison confirmed)

### keywords

- D:\hyli\kele-0707, episode-version-mismatch, episodes.character_objects, scenes.character_versions, shots.character_versions, speaker_character_versions, EpisodeContentWorkspace.jsx, PUT /api/episodes/{episodeId}, normalizeEpisodeCharacterObjects, allMatch: True

## Task 13: Trace `assign_off_screen_status` and off-screen object handling from automatic and manual entrypoints, success

### rollout_summary_files

- rollout_summaries/2026-07-27T06-45-07-5Woh-off_screen_object_handling_analysis_and_rebase_push.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\27\rollout-2026-07-27T14-45-07-019fa251-f2bf-7af3-b0b1-72574d96e121.jsonl, updated_at=2026-07-29T06:35:24+00:00, thread_id=019fa251-f2bf-7af3-b0b1-72574d96e121, analyzed the automatic/manual chain and pushed the later persisted off-screen object-handling rule)

### keywords

- D:\hyli\kele-0707, assign_off_screen_status, split_shots, assignOffScreenStatus, buildOffScreenShotInput, callShotOffScreenStatusProvider, rewrite_script, content-actions, assertOffScreenStatusPreservesScript, off-screen-object-handling-rule, providerSourceUrl, canonical shots, polish_shots

## Task 14: Recover local automated-workflow canvas read path after a generated-media 404 report, partial

### rollout_summary_files

- rollout_summaries/2026-07-29T01-30-15-uomJ-local_media_404_and_preserve_changes_git_pull.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\29\rollout-2026-07-29T09-30-15-019fab7e-673c-7f81-bb02-fb84856c5861.jsonl, updated_at=2026-07-30T02:36:45+00:00, thread_id=019fab7e-673c-7f81-bb02-fb84856c5861, restored Query and platform-admin propagation so the authenticated canvas loaded; original media-provider 404 remained unproven)

### keywords

- D:\hyli\kele-0707, generated-media-local-persistence, 404 Not Found, Query Service, 8092, Gateway, 8091, platformAdminBusinessPortal, X-Query-Actor-Platform-Admin-Business-Portal, workflow-query-service.ts, 325 canvas tasks, providerSourceUrl

## Task 15: Fix `49-1-1` version transitions and `19-2-1` physical-presence continuity, success

### rollout_summary_files

- rollout_summaries/2026-08-10T02-41-17-m1LI-kele0707_script_analysis_fixes_and_safe_latest_sync.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\10\rollout-2026-08-10T10-41-17-019fe98b-bdae-78f0-a7ad-01516f4933c2.jsonl, updated_at=2026-08-14T02:07:15+00:00, thread_id=019fe98b-bdae-78f0-a7ad-01516f4933c2, live page reproduced both outfit references, hardened analysis, pushed the focused fix, and later preserved dirty payment work during pull-only sync)

### keywords

- D:\hyli\kele-0707, 49-1-1, 19-2-1, 霍尔特, 休闲便装, 病号服, analyze_script, step 7, step 8, presentObjects, physical-presence-timeline.ts, analyze-script-step7-version-validation.js, enter-exit.ts, transition-note-fallback.ts, 14365a1e, 119/119

## User preferences

- when the user reports an exact local media-persistence `404`, use the authenticated browser page plus persisted task/provider data rather than guessing URLs; distinguish canvas recovery from proving the original media path is fixed [Task 14]

- when the user points at a visible mismatch and asks to keep going, keep tracing until the exact UI/read-model discrepancy is explained; do not stop at the first plausible backend guess [Task 1]
- when the user asks to use the Chrome logged-in session, prioritize the authenticated Chrome session instead of inventing alternate data paths when backend/API access is gated [Task 1]
- when browser-backed debugging is blocked and the user asks to install/enable the Codex Chrome Extension, unblock that path first and continue with the same session rather than falling back to guesswork [Task 1]
- when the user asks for a Chinese commit message and push, use a Chinese commit message and carry the work through push instead of stopping after local verification [Task 4][Task 6][Task 7][Task 15]
- when the user asks why two visible labels or texts disagree, trace the exact persisted write path and ownership chain first instead of answering as a UI-only mismatch [Task 5][Task 6][Task 7]
- when the user asks why visible opposite version transitions appear, trace the authenticated live page, exact task/API steps, structured data, and persistence chain; the presence of both references alone does not prove which provider step emitted the markers [Task 15]
- when the user reports an episode scene/section version that disagrees with episode content or extracted objects, follow the full episode -> scene -> section -> object extraction path and establish the persisted cause before patching [Task 10]
- when the user asked to "find the cause" and "check the real database data chain" for episode/scene/shot disagreement, compare persisted canonical version IDs across all three layers and refresh the UI; a matching label alone is not acceptance [Task 12]
- when the user approves "calculate a transition-relative offset only when `从`/`进入`/`变成` is actually present, return zero otherwise, add a real regression, and do not rewrite historical data", keep the repair narrow to future processing and prove the pre-fix failure with a behavior-level regression [Task 9]
- when the user says `关闭superpower`, do not invoke the superpowers workflow again in this checkout unless they explicitly re-enable it [Task 6]
- when the user asks how an existing feature is concretely implemented, trace the complete entrypoint -> input -> LLM call -> validation -> persistence/readback chain, distinguish automatic embedded execution from manual single-shot rewriting, and do not turn an analysis request into a code-change proposal [Task 13]

## Reusable knowledge

- In this checkout, the visible right-side object/reference slots are not necessarily the source of extra object enter-marker lines; if the active contenteditable already contains them, inspect `structuredSegments` / `presentObjects` first [Task 1]
- The traced path for this bug family is `scene.structured_segments` -> `buildInitialShots()` -> `insertObjectEnterMarkers()` -> `splitShotsFromStructuredScript(...)` -> `finalizeShotDisplayFields()` / polish, so wrong `presentObjects` values can survive all the way to visible shot text [Task 1]
- `services/provider-gateway/split-shots-provider.ts` passes project-wide location/other-object candidate pools into `splitShotsFromStructuredScript(...)`; this is relevant when object names look unrelated to the visible slot selection [Task 1]
- The standalone-exit-marker fix for shot content was intentionally display-only: `ManualVideoPromptPanel.jsx`, `StructuredScriptPanel.jsx`, and `ShotCard.jsx` gained a hide/filter path, while `src/features/automated-workflow/utils/shotExitMarkerDisplay.js` centralized detection of standalone lines or segments so persisted `script_text`, `structured_segments`, `enterExitInfo`, and provider inputs stay unchanged [Task 4]
- The durable regression seam for this exit-marker display contract is `tests/shot-exit-marker-display.test.mjs`, which covers the helper, editor projection, and the “only shot content opts in” boundary; rerun it together with `pnpm ts-check` and `git diff --check` when this display behavior changes [Task 4]
- When a persisted automated-workflow `split_shots` failure shows output-count mismatch, inspect the browser task detail first: in the validated `45-3` failure the provider output was only `<<<小节1>>>` with `contentLength: 9`, so the parser dropped it into `0` shots because there was no displayable `scriptText` [Task 5]
- The fix for that bug family was two-part: add a fast-fail guard in both split-shot code paths when finalized shots have empty display text, and normalize pure non-body scenes so they merge into adjacent body scenes instead of surviving as standalone scenes [Task 5]
- The workflow/UI read path for structured-script version mismatches is not purely textual: the script text can preserve a stale marker while canonical version fields come from a different normalized source, so inspect `analyze_script` step 7 enrichment, step 8 present-object alignment, and final version injection together [Task 6]
- `services/worker/structure/analyze-script-step7-version-validation.js` is now the boundary that rejects version ids or names not owned by the exact object under the exact type, and `services/worker/structure/analyze-script-structure-version-utils.js` must also rewrite already-versioned enter/exit markers to the segment-owned canonical replacement [Task 6]
- For `49-1-1`, Step 7 version output is untrusted enrichment: enforce exact object type, canonical identity, and version ownership, then rewrite existing enter/exit markers to the segment-owned canonical version so stale labels cannot survive injection [Task 15]
- Step 8 `presentObjects` normalization applies the ordered physical-presence timeline to fresh indexed provider responses and restored checkpoints. Character omission alone is not exit; only explicit character-associated departure, authoritative enter/exit markers, or a scene boundary ends presence. Local speakers are defensive presence evidence; remote/OS/VO speech remains excluded. `enter-exit.ts` reapplies the same normalization before marker generation, and a later legal re-entry remains possible [Task 15]
- Historical persisted `19-2-1` data was not rewritten. Rerun `analyze_script -> split_scenes -> split_shots` to apply this future-analysis behavior to historical episodes. Focused tests passed `119/119`, with `pnpm ts-check`, `pnpm build`, and `git diff --check` also passing before commit `14365a1ebd6edac6f3ffc417eadabbddce1c4f5c` (`修复剧本版本与角色在场识别`) was pushed [Task 15]
- Episode `versionIds` are a stricter boundary than project-wide object ownership: pass bound version ID/text through both live and frozen Step-7 inputs, filter prompt candidates to those bindings, and server-reject a selected version outside the episode. When exactly one bound version exists, validation may fall back to it; objects without bindings retain legacy full-project inference [Task 10]
- The focused Step-7 binding check set is `tests/analyze-script-step7-version-ownership-validation.test.mjs`, `tests/workflow-task-frozen-structure-input.test.mjs`, `tests/analyze-script-present-objects-version-input.test.mjs`, and `tests/analyze-script-worker-service-helper.test.mjs`, run with the TypeScript loader; this run passed 20 focused tests, `pnpm ts-check`, and `git diff --check` [Task 10]
- A `split_shots` error with an empty displayable first subsection can originate before provider submission: compare current canonical scene body segments with the frozen `task_start` snapshot. If filtering makes the snapshot empty, recovery commit `d6d57872` rebuilds it from current canonical input under `task_start:split_shots_recovery_v1`; do not present the remote snapshot detail as directly proven when it was not read [Task 11]
- For historical episode-version drift, distinguish future prevention from data repair. The Step-7 binding fix prevents new cross-episode choices but does not migrate old `episodes.character_objects`. Compare `episodes.character_objects` with `scenes.character_versions`, `shots.character_versions`, and `speaker_character_versions`; where scene/shot state is canonical, repair only the stale episode bindings through `PUT /api/episodes/{episodeId}` using `characterObjects`, then verify the API comparison returns `allMatch: True` and refresh the browser [Task 12]
- `appearance_episodes` is applicability metadata, not a replacement for actual episode/scene/shot bindings. `EpisodeContentWorkspace.jsx` reads `episode.characterObjects || episode.character_objects`, so an episode card mismatch can be persisted data rather than cache [Task 12]
- For remote pages on this network, local `.env.local`, Node `pg`, or unauthenticated HTTP reads can be supporting evidence only; if the visible page is actually served from `http://<internal-host>:3000/...`, the decisive proof path is the signed-in browser session [Task 6]
- In this repo, `split_shots` completion is a composite boundary: off-screen assignment, polish, canonical shot writeback, and post-write readback all matter, so `scene-shot-off-screen-status.ts` is the right guard point for script-body preservation and shot-number identity before polish/writeback [Task 7]
- The `38/39` live incident confirmed that `split_shots` is a compound completion boundary: split candidates, embedded `assign_off_screen_status`, embedded `polish_shots`, final canonical-shot write, and canonical readback must all pass [Task 8]
- The version-prefix root cause was `lastIndexOf(token) + token.length`: an absent transition token gives `-1 + 2`, a positive offset that leaves the start of a protected long versioned name exposed. `offsetAfterLastToken(text, token)` must return `0` when no actual token match exists [Task 9]
- The regression in `tests/analyze-script-version-injection-ownership.test.mjs` covers short `宋志远` inside a longer already-versioned object name and asserts both `originalFormat` and `actionContent`; the validated check set was 19 focused tests, `pnpm ts-check`, and `git diff --check` [Task 9]
- The `d955dc0c` fix affects future analysis only; existing corrupted canonical data needs re-running analysis and downstream split stages rather than a historical-data rewrite [Task 9]
- Retryable workflow classification for new structure-LLM failures has to be updated in both `services/worker/workflow-task-worker-writes.ts` and `src/lib/workflow/task-execution-result-utils.ts` or `default-task-executors.ts`; otherwise the new error will not re-enter the existing retry flow [Task 7]
- The highest-value regression for the off-screen bug family is `tests/shot-off-screen-status-integrity.test.mjs`, which proves the model may only prepend approved status lines and may not rewrite the original script body; leave `src/lib/workflow/shot-off-screen-status-prompt.ts` unchanged when the fix belongs in return validation [Task 7]
- `split_shots` embeds `assign_off_screen_status.llm` between candidate splitting and `polish_shots.llm`, then writes and reads back canonical `shots`; `buildOffScreenShotInput()` prefers `structuredSegments.presentObjects.characters` and falls back to canonical ids/version ids. The manual `POST /api/projects/{projectId}/shots/{shotId}/content-actions` with `rewrite_script` reuses the same module for one shot and patches only `script_text` [Task 13]
- The dependable verification set for this automated-workflow display/structure area remains focused Node contract tests plus `pnpm ts-check` and `git diff --check`; broader prompt-parity failures may be unrelated and should not automatically be blamed on the current fix [Task 4][Task 5]
- For browser-backed evidence gathering here, the reliable proof path was the logged-in page plus structure/contenteditable inspection when page-sandbox `fetch` was unavailable and `domSnapshot()` failed [Task 1]
- The push workflow that worked in this checkout was: stage only the intended files, commit with a Chinese message, `git credential reject` when HTTP auth is stale, `git stash push --include-untracked`, `git fetch origin main`, `git rebase origin/main`, `git push origin main`, then `git stash pop` to restore unrelated local edits [Task 4][Task 15]
- The local automated canvas needs Web `3333`, Gateway `8091`, Query `8092`, NATS monitor `8224`, and PostgREST. A missing Query Service breaks the canvas read path even when the web page is available [Task 14]
- Business-portal platform-admin access must survive Gateway -> Query as `platformAdminBusinessPortal`: Gateway forwards `X-Query-Actor-Platform-Admin-Business-Portal`, and Query rechecks the database platform role before cross-workspace reads. Relevant points: `services/query/gateway-auth-service.ts`, `services/gateway-go/main.go`, `services/query/workflow-query-service.ts` [Task 14]
- After Query recovery, the authenticated Chrome page showed 325 canvas tasks with visible nodes and edges and no browser warning/error logs; existing failed task counts were not retried or charged again [Task 14]
- In the later exit-marker fix run, the same host needed `git credential reject` first and then `git fetch origin main` -> `git rebase origin/main` -> `git push origin main`; this checkout’s safe default is still rebase-and-retry, not force-push [Task 4]

## Failures and how to do differently

- Symptom: an automated canvas fails to load while Web is up. Cause: Query Service is absent or Gateway-to-Query platform-admin context is lost. Fix: narrow checks to required ports/processes/logs, restore Query `8092`, then verify the authenticated canvas rather than doing a broad process scan [Task 14]
- Symptom: canvas recovery is treated as resolution of a generated-media `404`. Cause: the media path was not independently reproduced to a final provider URL/root cause. Fix: report the canvas result separately and reproduce media generation before claiming the 404 is fixed [Task 14]

- Symptom: unrelated object enter-marker lines look like a slot-display bug. Cause: the corruption may already be in structured-segment `presentObjects`, which `insertObjectEnterMarkers()` will faithfully propagate. Fix: inspect the active contenteditable plus structure-tab `presentObjects` before rewriting display code [Task 1]
- Symptom: browser automation cannot attach even though Chrome is running. Cause: the Codex Chrome Extension may be missing or disabled in the selected profile. Fix: verify extension installation/enabled state first, then open the webstore page and `chrome://extensions/` if needed [Task 1]
- Symptom: both opposite transition labels are visible, but no task/API-step attribution was recovered. Cause: live UI/reference evidence was observed without completing the task-step trace. Fix: report the two transitions as reproduced but do not claim a specific provider step until authenticated task/API-step and persistence evidence is collected [Task 15]
- Symptom: source-inspection tests fail after a provider-context migration while provider request assertions pass. Cause: stale tests still point at the previous entry file. Fix: locate the moved SQL/context surface (here `split-shots-provider-context.ts`) and keep unrelated stale tests out of the focused-fix claim [Task 15]
- Symptom: direct page-sandbox probing fails with `fetch is not a function` or `domSnapshot()` errors. Cause: the current page/browser sandbox does not expose those APIs reliably. Fix: fall back to `tab.dom_cua.get_visible_dom()` plus targeted `evaluate()` reads of visible text and contenteditable content [Task 1]
- Symptom: a split-shot mismatch gets misdiagnosed as canonical backend corruption. Cause: stale `shots.object_slots` can override canonical ids/versions on the read side. Fix: start with a focused UI read-support regression test and only treat `object_slots` as authoritative when its object/version selection set matches canonical fields [Task 2]
- Symptom: standalone exit markers show up as their own visible shot-content rows even though they should only decorate nearby content. Cause: the display layer needs to hide/filter standalone marker rows or segments that are not attached to narrative text in the shot-content editors. Fix: patch the read-support projection with the shared `shotExitMarkerDisplay.js` helper instead of mutating persisted shot text or provider input [Task 4]
- Symptom: `split_shots` fails with "expected 1, actual 0" even though the provider did return text. Cause: the returned payload can be marker-only and collapse to empty `scriptText`, or a pure non-body scene can survive long enough to create an empty display shot. Fix: inspect the browser detail payload, then enforce the empty-display fast-fail and adjacent-body merge rules before any paid embedded-text step [Task 5]
- Symptom: local DB inspection proves nothing about the failing workflow task. Cause: the target project/task may live only in the remote browser environment, not the local `.env` database. Fix: switch to the logged-in browser evidence path instead of insisting on local DB proof [Task 5]
- Symptom: aggregate failed counts or unrelated red nodes appear to explain an exact episode failure. Cause: visible canvas errors can belong to another group while the named node is still running or serially pending. Fix: inspect exact IDs such as `split_shots_38_1` and `split_shots_39_1`, then wait for terminalization or query the correct live runtime and correlate task error, API steps, provider ledger, shot persistence, and readback before asserting a root cause [Task 8]
- Symptom: the UI shows one object version while the script text still shows another. Cause: `analyze_script` step 7 accepted a plausible-looking version from the wrong object/type and final marker rewrite preserved that stale label in text. Fix: treat step-7 provider output as untrusted enrichment, enforce exact object/type/version ownership, and rewrite existing enter/exit markers to the segment-owned canonical label [Task 6]
- Symptom: a same-object version shown in scene/section content becomes a different version in episode content or extracted objects. Cause: Step 7 received project-wide candidates but did not enforce the episode's `versionIds`; later normalization persisted the model-selected wrong version. Fix: enforce the episode binding in prompt candidates and server validation, and inspect the authenticated page when unauthenticated API requests return `401` [Task 10]
- Symptom: `split_shots provider failed` says the first subsection has no displayable script text even though current scene data has body text. Cause: an old frozen `task_start` snapshot retained only non-body and enter-marker content; display filtering emptied it before provider submission. Fix: inspect task error plus current canonical input and snapshot recovery path, and recover from current canonical input when the frozen snapshot is unusable [Task 11]
- Symptom: episode cards show obsolete outfits while scene/shot data and structured script agree on newer versions. Cause: historical `episodes.character_objects` is stale; the future Step-7 guard does not migrate old rows. Fix: use the real episode/scene/shot API chain, update only the episode's stale `characterObjects`, verify IDs across layers, and wait for the asynchronously loaded episode tree before judging a browser refresh [Task 12]
- Symptom: local DB queries or `.env.local` assumptions do not explain a visible automated page mismatch. Cause: the page may be served from a remote host and authenticated browser state rather than the local checkout runtime. Fix: confirm the actual page host first and prefer the signed-in browser session for decisive evidence [Task 6][Task 7]
- Symptom: off-screen-status output rewrites shot body text or returns duplicate/unknown shot numbers. Cause: provider output was accepted too loosely at the `assignOffScreenStatus()` boundary. Fix: enforce exact original-body preservation plus 1:1 shot identity, mark violations retryable, and verify with the focused integrity test rather than broad parity suites [Task 7]
- Symptom: a first integrity patch rejects valid cases where the original script already contains a status line. Cause: the guard strips prefixes too aggressively instead of checking the allowed-prefix plus original-body shape. Fix: accept either exact equality or normalized allowed-prefix lines prepended to the unchanged original body [Task 7]
- Symptom: the feature is described as a deterministic local role-presence engine. Cause: semantic presence is LLM-owned; server checks instead enforce expected count, shot identity, original-body preservation, and canonical postconditions. Fix: state the prompt-contract versus deterministic-validation boundary explicitly; source-regex test failures can be stale even when focused integrity tests pass [Task 13]
- Symptom: push fails after a clean local commit. Cause: first stale HTTP credentials, then a moved `origin/main`. Fix: clear cached host credentials, stash unrelated edits when needed, rebase onto the latest remote, and retry push instead of force-pushing [Task 3][Task 4]

# Task Group: D:\hyli\kele-0707 local dev stack restart, automatic migrations, and runtime verification
scope: Recover the full local workflow stack in `D:\hyli\kele-0707`, including automatic migration, process cleanup, and Web/Gateway/NATS/PostgREST verification.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse only for this checkout's current `.runtime` scripts, ports, and local DB; re-check helpers, effective `.env` / `.env.local` overrides, and listener ownership if the stack layout changes.

## Task 1: Diagnose episode 26 `analyze_script` failure and persist automatic full local-stack restart, success

### rollout_summary_files

- rollout_summaries/2026-07-16T03-00-17-DJbF-episode_26_script_analysis_and_full_auto_restart.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\sessions\2026\07\16\rollout-2026-07-16T11-00-22-019f68de-26a8-7783-b561-2978ff19045f.jsonl, updated_at=2026-07-17T02:07:33+00:00, thread_id=019f68de-26a8-7783-b561-2978ff19045f, captured the failed live task, repaired schema/runtime prerequisites, and verified complete local restart)

### keywords

- analyze_script_ep26, workflow_task_execution_is_authorized, schema_migrations, AUTO_MIGRATE_ON_DEV, restart-local-stack.ps1, PostgREST, workflow-task-worker, workflow-task-scheduler, psycopg.errors.UndefinedFunction

## Task 2: Restart the local dev stack from stale repo-owned listeners and verify the full runtime, success

### rollout_summary_files

- rollout_summaries/2026-07-08T06-42-55-WYou-kele0707_diagnostic_sync_and_dev_restart.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-08T14-43-02-019f4077-1d2f-7bf0-a301-39788878b0ae.jsonl, updated_at=2026-07-10T06:44:59+00:00, thread_id=019f4077-1d2f-7bf0-a301-39788878b0ae, stopped repo-owned listeners safely, relaunched with Git Bash, and rechecked ports, endpoints, and worker logs)
- extensions/ad_hoc/notes/20260708-154925-kele0707-dev-restart.md (cwd=D:\hyli\kele-0707, rollout_path=ad_hoc_note, updated_at=2026-07-08, thread_id=ad_hoc_note, full local dev-stack restart/verification note) [ad-hoc note]
- extensions/ad_hoc/notes/20260707-162726-kele0707-local-dev-restart.md (cwd=D:\hyli\kele-0707, rollout_path=ad_hoc_note, updated_at=2026-07-07, thread_id=ad_hoc_note, Git Bash workflow-runtime restart note) [ad-hoc note]

### keywords

- D:\hyli\kele-0707, .runtime/start-dev-with-local-bin.sh, scripts/dev.sh, Get-NetTCPConnection, workflow-task-scheduler.log, workflow-task-worker.log, workflow-readmodel-projector.log, gateway-go.log, 3333, 8091, 4224, 8224, DEV_WORKFLOW_WORKER_ENABLED=false

## Task 3: Restart the local dev stack with repo-local runtime PATH and full-stack verification, reference note

### rollout_summary_files

- extensions/ad_hoc/notes/20260707-162726-kele0707-local-dev-restart.md (cwd=D:\hyli\kele-0707, rollout_path=ad_hoc_note, updated_at=2026-07-07, thread_id=ad_hoc_note, Git Bash workflow-runtime restart note) [ad-hoc note]
- extensions/ad_hoc/notes/20260708-154925-kele0707-dev-restart.md (cwd=D:\hyli\kele-0707, rollout_path=ad_hoc_note, updated_at=2026-07-08, thread_id=ad_hoc_note, full local dev-stack restart/verification note) [ad-hoc note]

### keywords

- D:\hyli\kele-0707, .runtime/start-dev-with-local-bin.sh, .runtime/start-workflow-runtime.sh, workflow-gateway.exe, nats-server.exe, 3333, 8091, 4224, 8224, dev-restart-stdout.log, PGSSLMODE=disable, Git Bash

## Task 4: Restart the full Windows local stack after latest `main` and verify all runtime services, success

### rollout_summary_files

- rollout_summaries/2026-07-22T07-51-53-Ag1U-present_objects_prompt_optimization_git_sync_local_stack_res.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\22\rollout-2026-07-22T15-51-58-019f88cf-4962-7a33-8909-13eda5368f46.jsonl, updated_at=2026-07-27T03:33:20+00:00, thread_id=019f88cf-4962-7a33-8909-13eda5368f46, restarted the full local stack after fast-forwarding `main` and validated listeners, endpoints, processes, and logs)

### keywords

- D:\hyli\kele-0707, restart-local-stack.ps1, AUTO_MIGRATE_ON_DEV, Node 26, file:///D:/, 3333, 8091, 4224, 8224, 55432, 55437, media-worker, outbox-publisher, gateway-go

## Task 5: Complete local development-stack restart after Git sync, success

### rollout_summary_files

- rollout_summaries/2026-07-22T07-51-53-Ag1U-present_objects_prompt_optimization_git_sync_local_stack_res.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\22\rollout-2026-07-22T15-51-58-019f88cf-4962-7a33-8909-13eda5368f46.jsonl, updated_at=2026-07-27T03:33:20+00:00, thread_id=019f88cf-4962-7a33-8909-13eda5368f46, restarted the full local stack after Git sync and validated listeners, endpoints, processes, and logs)

### keywords

- D:\hyli\kele-0707, restart-local-stack.ps1, AUTO_MIGRATE_ON_DEV, workflow_task_execution_is_authorized, 3333, 8091, 4224, 8224, 55432, 55437, NO_STARTUP_FATALS_FOUND, SyntaxError: Invalid or unexpected token

## Task 6: Validate screen-text repair and restart the local full stack, success

### rollout_summary_files

- rollout_summaries/2026-08-04T09-17-58-OzVk-analyze_script_screen_text_local_fix_and_runtime_restart.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\08\04\rollout-2026-08-04T17-17-58-019fcc10-c255-74d3-b4ab-61200600c03e.jsonl, updated_at=2026-08-04T10:50:30+00:00, thread_id=019fcc10-c255-74d3-b4ab-61200600c03e, focused validation and repository restart script verified local services)

### keywords

- D:\hyli\kele-0707, restart-local-stack.ps1, c047bd9f, 41/41, pnpm ts-check, 3333, 8091, 4224, 8224, 55432, 55437, workflow-task-scheduler, workflow-task-worker, workflow-readmodel-projector, DEV_WORKFLOW_WORKER_ENABLED=false

## User preferences

- when the user gave a project URL and episode number, the investigation was expected to trace the real workflow task rather than infer from the canvas label -> identify node, episode, run, task, and API-step evidence first [Task 1]
- when the user said `自动迁移，以后重启都完整重启`, use a complete dependency-aware restart with migrations, not only Next.js or a one-service repair [Task 1]
- in `D:\hyli\kele-0707`, the user expects the full local runtime stack to come back, not just a `Next` `200 OK`; verify web, gateway, NATS, and workflow workers together [Task 2][Task 3] [ad-hoc note]
- when asked to restart local development services after a repair, run the repository's complete restart procedure and check Web, PostgREST, NATS, Gateway, Scheduler, Worker, Projector, outbox publisher, and media worker rather than only the web server [Task 6]
- when the user asks for a `完整重启本地开发服务`, include migrations and validate the whole runtime, not only Next.js on `3333` [Task 5]

## Reusable knowledge

- The formal API-step read path is `/api/workspaces/{workspaceId}/workflow-runs/{runId}/tasks/{taskId}/api-steps`; for episode 26, live Chrome-authenticated evidence proved steps 1-5 succeeded before `analyze_script.workflow_task_worker_failure`, while the checkout's default `.env` DB had zero project tasks [Task 1]
- Missing `public.workflow_task_execution_is_authorized(uuid)` with 11 pending migrations caused Worker exit and scheduler `psycopg.errors.UndefinedFunction`; effective final `AUTO_MIGRATE_ON_DEV=true` overrides enabled the successful migration/restart path [Task 1]
- Use `powershell .\\.runtime\\restart-local-stack.ps1`; it waits for PostgreSQL/PostgREST, stops runtime processes across Windows, slash, and Git Bash path forms, and launches the full stack. Related skill: skills/kele0707-full-stack-restart/SKILL.md [Task 1]
- On Windows, do not globally add PostgreSQL `bin` to `PATH`: that selected the shell/psql branch and hit `20260420_add_scripts_image_resolution_columns.sql` CRLF/LF checksum mismatch. Invoke PostgreSQL binaries by absolute path so the Node migration runner normalizes line endings [Task 1]
- Plain `pnpm dev` can bring up Next on `3333` while missing repo-local runtime dependencies such as `.runtime/bin/nats-server.exe`; use the repo-local path and verify `3333`, `8091`, `4224`, `8224`, plus PostgREST `55437` [Task 2][Task 3] [ad-hoc note]
- For workflow-runtime-only recovery, start with Git Bash `& 'C:\Program Files\Git\usr\bin\bash.exe' .runtime/start-workflow-runtime.sh`; it must restore `/usr/bin:/bin`, append `.runtime/bin`, export runtime DB/PostgREST/NATS variables, and set `PGSSLMODE=disable` [Task 3] [ad-hoc note]
- Successful full-stack verification included `pending=0`, authorization function present, HTTP 200 for Web health/project page/Gateway/NATS/PostgREST, expected service processes/listeners, and targeted Scheduler/Worker/Projector/Gateway/NATS logs without startup errors [Task 1]
- The current standard command is `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\.runtime\\restart-local-stack.ps1`. A successful restart had all six listeners (`3333`, `8091`, `4224`, `8224`, `55432`, `55437`), HTTP 200 from Web, Gateway `/healthz`, NATS `/varz`, and PostgREST root, plus Scheduler, Worker, Projector, media-worker, outbox-publisher, and gateway-go still running without fatal startup errors [Task 4]
- On Node 26, preserve the `file:///D:/...` loader URL convention; raw `/d/...` or `D:/...` import paths regress startup [Task 4]
- The complete acceptance set is six listeners (`3333`, `8091`, `4224`, `8224`, `55432`, `55437`); HTTP 200 for Web root, Gateway `/healthz`, NATS `/varz`, and PostgREST root; migrations up to date; authorization function present; Scheduler, media-worker, outbox-publisher, workflow-task-scheduler/worker/projector, Gateway, and NATS running; and no `Traceback`, `FATAL`, `UndefinedFunction`, ESM URL error, `panic:`, or `failed to start` in recent logs [Task 5]
- After the source-integrity repair, `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\.runtime\\restart-local-stack.ps1` restored the six listeners and returned HTTP 200 from Web, PostgREST, and NATS. Logs confirmed Scheduler, task worker, projector, outbox publisher, media worker, and gateway; legacy Node workflow-worker was intentionally skipped because `DEV_WORKFLOW_WORKER_ENABLED=false`. [Task 6]

## Failures and how to do differently

- Symptom: an authenticated live page fails while local audit returns zero project tasks. Cause: the page's service/database does not match default local `.env`; direct unauthenticated calls also return 401. Fix: use browser-authenticated node/detail evidence, then continue with the captured task/run IDs through API-step details or matching logs [Task 1]
- Symptom: worker exits and scheduler throws `psycopg.errors.UndefinedFunction`. Cause: schema drift missing `workflow_task_execution_is_authorized(uuid)`. Fix: correct effective migration overrides, run the full restart, and verify migrations and function existence [Task 1]
- Symptom: a restart leaves duplicate Scheduler, Worker, or Projector processes. Cause: cleanup missed `D:/hyli/kele-0707` or `/d/hyli/kele-0707` command forms. Fix: match all three path representations and repeat stop rounds until no runtime process/listener remains [Task 1]
- Symptom: migration rejects CRLF/LF checksum mismatch. Cause: global PostgreSQL `PATH` switched runners. Fix: preserve the Node migration path with absolute PostgreSQL binary paths [Task 1]
- Symptom: the web app loads on `3333` but workflow runtime pieces are missing. Cause: bare web startup missed repo-local tooling. Fix: use `.runtime` startup scripts and verify all expected ports/processes [Task 2][Task 3] [ad-hoc note]
- Symptom: workflow TypeScript services launched from Windows `Start-Process` fail due to path/URL conversion, SSL, or missing shell utilities. Cause: raw Windows startup or sourced env `PATH` clobbering. Fix: use Git Bash, export `PGSSLMODE=disable`, and restore `/usr/bin:/bin` after sourcing env files [Task 3] [ad-hoc note]
- Symptom: a read-only database verification reports `SyntaxError: Invalid or unexpected token`. Cause: PowerShell quoting truncated the inline Node SQL string, not a broken database. Fix: pipe a PowerShell here-string to `node` and then assess the result; do not fail the restart on an early Gateway sample before the full startup wait and combined checks [Task 5]
- Symptom: local restart is taken as proof of production recovery. Cause: the restart only affects `127.0.0.1:3333`; remote `<internal-host>:3000` stayed undeployed/unrerun. Fix: keep local runtime verification and remote deployment/rerun as separate completion claims. [Task 6]

# Task Group: D:\hyli\kele-0707 git sync, push, and Chinese commit-message workflow
scope: Carry `D:\hyli\kele-0707` work through staged-only Chinese local commits, stale-auth recovery, repeated fetch/rebase, and final `main` sync confirmation when the user asks to commit or push.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse for this checkout's HTTP-remote git maintenance when the user asks for local Chinese commit plus push or latest-sync-plus-push; re-check remote drift, credential state, and verification results if the checkout or remote moved.

## Task 1: Commit and push focused `kele-0707` fixes with Chinese messages after auth or rebase recovery, success

### rollout_summary_files

- rollout_summaries/2026-07-16T09-13-10-Qnz0-split_shots_off_screen_integrity_and_chinese_commit_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\sessions\2026\07\16\rollout-2026-07-16T17-13-16-019f6a33-8c25-7f71-84b4-dad6788644b6.jsonl, updated_at=2026-07-16T10:33:55+00:00, thread_id=019f6a33-8c25-7f71-84b4-dad6788644b6, rebased a Chinese local fix onto moving `origin/main`, reran focused checks, and pushed with final `0 0` divergence)
- rollout_summaries/2026-07-16T02-55-38-xT95-analyze_script_version_ownership_fix_commit_rebase_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\sessions\2026\07\16\rollout-2026-07-16T10-55-43-019f68d9-e51d-76f3-8097-526b12189f47.jsonl, updated_at=2026-07-16T09:19:15+00:00, thread_id=019f68d9-e51d-76f3-8097-526b12189f47, carried a Chinese local fix through remote-url repair, rebase conflict resolution, and clean push on `main`)
- rollout_summaries/2026-07-10T10-14-01-am44-hide_shot_exit_markers_display_and_push.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-10T18-14-01-019f4b85-193b-7902-917d-88585adcf774.jsonl, updated_at=2026-07-14T07:28:41+00:00, thread_id=019f4b85-193b-7902-917d-88585adcf774, repeated the same Chinese commit plus auth-recovery push pattern on the same host)

### keywords

- D:\hyli\kele-0707, 中文本地提交并推送, Chinese commit message, git credential reject, git fetch origin main, git rebase origin/main, git push origin main, stale HTTP auth, origin/main drift, 0 0, quyuan/kele.git

## Task 2: Rebase the current Chinese local fix onto latest `origin/main` and push non-interactively, success

### rollout_summary_files

- rollout_summaries/2026-07-14T06-09-27-Cp4Z-split_scenes_migrations_and_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\archived_sessions\rollout-2026-07-14T14-09-32-019f5f3e-9ebb-7db3-8aca-723d3e327dc9.jsonl, updated_at=2026-07-14T08:35:55+00:00, thread_id=019f5f3e-9ebb-7db3-8aca-723d3e327dc9, rebased one Chinese commit across a docs conflict and pushed with non-interactive git settings)

### keywords

- D:\hyli\kele-0707, git -c credential.interactive=never, fetch origin main, rebase origin/main, docs conflict, workflow-active-chains-cn.md, git rev-list --left-right --count, main -> main

## Task 3: Pull latest `main` while preserving unrelated local modifications, success

### rollout_summary_files

- rollout_summaries/2026-07-20T06-13-10-xn3C-kele_0707_split_shots_diagnosis_version_span_fix_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\20\rollout-2026-07-20T14-13-16-019f7e28-30ce-7963-8105-68d9265886e4.jsonl, updated_at=2026-07-21T08:51:45+00:00, thread_id=019f7e28-30ce-7963-8105-68d9265886e4, restored auth, fast-forwarded main, and retained three unrelated files)

### keywords

- D:\hyli\kele-0707, git credential-manager erase, git stash push --include-untracked, git pull --ff-only origin main, git stash pop, acab68a3, origin/main, 0 0, docs/README.md, scripts/dev.sh

## Task 4: Commit the presentObjects prompt fix in Chinese and push `main`, success

### rollout_summary_files

- rollout_summaries/2026-07-22T07-51-53-Ag1U-present_objects_prompt_optimization_git_sync_local_stack_res.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\22\rollout-2026-07-22T15-51-58-019f88cf-4962-7a33-8909-13eda5368f46.jsonl, updated_at=2026-07-27T03:33:20+00:00, thread_id=019f88cf-4962-7a33-8909-13eda5368f46, staged only 10 task files, rebased, reran focused checks, and pushed `b91576e1` with final `0 0` divergence)

### keywords

- D:\hyli\kele-0707, 中文提交并推送, b91576e1, git credential reject, credential.interactive=never, git fetch origin main, git rebase origin/main, git push origin main, docs/README.md, scripts/dev.sh, docs/LOCAL-DEV-RESTART.md

- Related skill: skills/kele0707-git-sync/SKILL.md

## Task 5: Fast-forward a clean local `main` to latest `origin/main`, success

### rollout_summary_files

- rollout_summaries/2026-07-22T07-51-53-Ag1U-present_objects_prompt_optimization_git_sync_local_stack_res.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\22\rollout-2026-07-22T15-51-58-019f88cf-4962-7a33-8909-13eda5368f46.jsonl, updated_at=2026-07-27T03:33:20+00:00, thread_id=019f88cf-4962-7a33-8909-13eda5368f46, fast-forwarded clean `main` from `d6d57872` to `3d2be3c3` and confirmed `0 0` divergence)

### keywords

- D:\hyli\kele-0707, git pull --ff-only origin main, d6d57872, 3d2be3c3, Merge remote-tracking branch 'origin/main', credential.interactive=never, HEAD...origin/main, 0 0

## Task 6: Pull latest `main` after stale Git Credential Manager auth, success

### rollout_summary_files

- rollout_summaries/2026-07-22T09-59-03-QxgA-kele0707_version_mismatch_stale_split_shots_git_sync.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\22\rollout-2026-07-22T17-59-08-019f8943-b2da-7080-8f69-4128f5976f48.jsonl, updated_at=2026-07-27T06:45:36+00:00, thread_id=019f8943-b2da-7080-8f69-4128f5976f48, erased stale credential then fast-forwarded clean main to `52c15f95` and confirmed `0 0`)

### keywords

- D:\hyli\kele-0707, git credential-manager erase, git pull --ff-only origin main, <internal-host>:3001/quyuan/kele.git, 52c15f95, git ls-remote origin HEAD, HEAD...origin/main, 0 0

## Task 7: Preserve local Chinese commits through rebase and ordinary push when explicitly asked, success

### rollout_summary_files

- rollout_summaries/2026-07-27T06-45-07-5Woh-off_screen_object_handling_analysis_and_rebase_push.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\27\rollout-2026-07-27T14-45-07-019fa251-f2bf-7af3-b0b1-72574d96e121.jsonl, updated_at=2026-07-29T06:35:24+00:00, thread_id=019fa251-f2bf-7af3-b0b1-72574d96e121, rebased five local Chinese commits across worker modularization, preserved unrelated user changes, and pushed with final `0 0` divergence)

### keywords

- D:\hyli\kele-0707, git stash push --include-untracked, git fetch origin main, git rebase origin/main, credential.interactive=never, git push origin main, workflow-task-worker-split-shot-text-steps.ts, git rev-list --left-right --count, git ls-remote origin refs/heads/main, 9f10d9bd, stash@{0}

## Task 8: Pull latest `main` while preserving a large dirty worktree, success

### rollout_summary_files

- rollout_summaries/2026-07-29T01-30-15-uomJ-local_media_404_and_preserve_changes_git_pull.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\29\rollout-2026-07-29T09-30-15-019fab7e-673c-7f81-bb02-fb84856c5861.jsonl, updated_at=2026-07-30T02:36:45+00:00, thread_id=019fab7e-673c-7f81-bb02-fb84856c5861, fast-forwarded to `6dca48c6`, resolved three stash conflicts semantically, and restored 23 modified plus 4 untracked files)

### keywords

- D:\hyli\kele-0707, git credential reject, credential.interactive=never, git fetch origin main --prune, git stash push --include-untracked, git merge --ff-only origin/main, git stash pop, default-task-executors.ts, providerSourceUrl, pnpm ts-check, 6dca48c6, 0 0

- Related skill: skills/kele0707-git-sync/SKILL.md

## Task 9: Update the Gitea `origin` URL to `<internal-host>:3001`, success

### rollout_summary_files

- rollout_summaries/2026-08-10T08-20-05-baht-update_gitea_origin_remote.md (cwd=D:\hyli\kele-0707, rollout_path=%USERPROFILE%\.codex\sessions\2026\08\10\rollout-2026-08-10T16-20-05-019feac1-ec59-7d30-b6c9-7fcfb2f2cfbb.jsonl, updated_at=2026-08-10T08:27:26+00:00, thread_id=019feac1-ec59-7d30-b6c9-7fcfb2f2cfbb, changed fetch and push origin URLs while preserving dirty worktree files)

### keywords

- D:\hyli\kele-0707, git remote set-url, origin, Gitea, <internal-host>:3001, <internal-host>:3001, quyuan/kele.git, url.ssh://kele-git/.insteadof, SSH HostName

## User preferences

- when the user asks to pull latest with uncommitted work present, preserve all tracked and untracked edits, do not commit or discard them, and finish with an explicit synchronized-but-unstaged state [Task 8]

- when the user asks `中文本地提交，再推送`, keep the commit message in Chinese and carry the work through the actual push instead of stopping after local verification [Task 1]
- when the user asks for a local commit and push in this checkout, default to ordinary push on `main` after rebase-and-retry; do not propose force-push unless they explicitly ask for history rewriting [Task 1][Task 2]
- when the user asks to pull latest, keep the existing local Chinese commit, preserve the intended staged change set, and finish the sync through rebase plus final branch-status confirmation [Task 1][Task 2]
- when the user asks `拉取最新版，中文提交并推送`, stage only the task files, preserve unrelated worktree edits, use a Chinese commit subject, and actually push `main` without force-push [Task 4]
- when the user asks `拉取最新版`, execute the pull directly, repair Git Credential Manager state if needed, and complete remote verification [Task 6]
- when the user repeatedly asks only `拉取最新版`, preserve local commits and rebase onto newest `origin/main`, but stop before push unless the user explicitly asks to push; when they say `推送`, perform the ordinary `main` push without force-push [Task 7]

## Reusable knowledge

- The dirty pull-only path is `git credential reject` for the affected host -> `git -c credential.interactive=never fetch origin main --prune` -> `git stash push --include-untracked` -> `git merge --ff-only origin/main` -> `git stash pop` -> semantic conflict resolution -> reset the index if restored edits must remain unstaged. The validated run advanced `cb002128` to `6dca48c6` and ended at `main...origin/main` `0 0` [Task 8]
- In the validated large-worktree restore, keep both upstream and local behavior in `docs/audit/workflow-active-chains-cn.md`, `docs/audit/workflow-active-chains.md`, and `src/lib/workflow/default-task-executors.ts`; the runtime file retained persisted image recovery plus `providerSourceUrl`. `pnpm ts-check`, 18 targeted generated-image/media-persistence tests, and `git diff --check` passed [Task 8]

- The stable push workflow in this checkout is: verify the intended files -> stage only the intended files -> commit with a Chinese subject -> clear stale HTTP auth with `git credential reject` if needed -> `git fetch origin main` -> `git rebase origin/main` -> rerun focused verification after each rebase -> `git push origin main` -> confirm `git rev-list --left-right --count HEAD...origin/main` is `0 0` and `git status --short` is empty [Task 1]
- Git Credential Manager is active globally here (`credential.helper=manager`). In the historical `.18` run, the stale-auth symptom was `fatal: Authentication failed for 'http://<internal-host>:3001/quyuan/kele/'`, repaired with `git credential reject` for `protocol=http`, `host=<internal-host>:3001`; use the host currently shown by `origin` for a new failure [Task 1][Task 9]
- Earlier pull/push recovery used `http://<internal-host>:3001/quyuan/kele.git`; the current direct `origin` is `.28:3001`. If authentication fails, inspect the actual fetch/push URL and user-level rewrites before changing branch state [Task 1][Task 9]
- Remote drift can happen more than once while verification is running in `D:\hyli\kele-0707`; after long test, typecheck, or build runs, fetch `origin/main` again before pushing rather than trusting an earlier clean rebase [Task 1]
- When automation might hang on credential prompts, the non-interactive variant `git -c credential.interactive=never fetch origin main` / `git -c credential.interactive=never push origin main` is the safer default, especially if the remote already showed auth churn or prompt blocking [Task 2]
- The repeated successful pattern in this checkout keeps the branch clean by staging only the intended files and avoids force-push even when `origin/main` advances during the session [Task 1][Task 2]
- For a pull-only request with unrelated local edits, the validated preservation workflow is `git stash push --include-untracked` -> `git pull --ff-only origin main` -> `git stash pop`; the July run ended at `acab68a3` with `HEAD` and `origin/main` both synchronized (`0 0`) while `docs/README.md`, `scripts/dev.sh`, and `docs/LOCAL-DEV-RESTART.md` remained untouched [Task 3]
- In the July 22 commit-and-push run, `docs/README.md`, `scripts/dev.sh`, and `docs/LOCAL-DEV-RESTART.md` were pre-existing user changes and were deliberately excluded. The verified sequence was stage task files -> Chinese commit -> `git credential reject` -> non-interactive fetch/rebase -> focused checks -> non-interactive push -> refetch and confirm `HEAD...origin/main` is `0 0` [Task 4]
- With a clean worktree and no local commit to preserve, use non-interactive `fetch origin main` then `git pull --ff-only origin main`; verify `HEAD...origin/main` is `0 0` rather than treating an intermediate credential warning as the final result [Task 5]
- For a clean pull-only request, `git credential-manager erase` for the stale host entry can restore `git pull --ff-only origin main`; verify both `HEAD...origin/main` `0 0` and `git ls-remote origin HEAD`. This evidence fast-forwarded to `52c15f95`; it did not create or push a commit [Task 6]
- In a moving-remote session, re-fetch immediately before push even after a successful rebase and focused checks. The final verified push used non-interactive fetch/push, then `git rev-list --left-right --count HEAD...origin/main` `0 0`, matching `git ls-remote origin refs/heads/main` and a clean worktree [Task 7]
- Current direct `origin` fetch and push URLs are `http://<internal-host>:3001/quyuan/kele.git`, set with `git remote set-url origin http://<internal-host>:3001/quyuan/kele.git`; preserve any pre-existing modified or untracked files while changing only the remote configuration [Task 9]
- When a rebase conflict lands in a one-line compressed service file, the reliable resolution is to take the updated upstream version as the base and reapply the local logic, then rerun the focused tests plus `git diff --cached --check` before continuing [Task 1]

## Failures and how to do differently

- Symptom: `git stash pop` conflicts after a dirty pull-only fast-forward. Cause: upstream and local changes overlap in runtime recovery or audit behavior. Fix: merge behavior from both sides, validate, drop only the temporary sync stash, and retain restored user edits unstaged/uncommitted [Task 8]

- Symptom: `git fetch` or `git push` fails with `fatal: Authentication failed`. Cause: stale HTTP credential state in GCM for the host actually configured by `origin`; `.18` was the historical instance. Fix: clear that host with `git credential reject` before reasoning about branch divergence or retrying the same command [Task 1][Task 9]
- Symptom: `git pull --rebase origin main` still fails even after credential repair. Cause: `origin`, a URL rewrite, or SSH alias can still target the wrong endpoint. Fix: inspect `git remote -v`, global `url.*.insteadof`, and SSH config; use the current direct `origin` `.git` URL `http://<internal-host>:3001/quyuan/kele.git` before retrying the rebase [Task 1][Task 9]
- Symptom: `git push origin main` is rejected again after a successful rebase. Cause: `origin/main` advanced during verification, so the previous rebase is already stale. Fix: fetch again, rebase again, rerun the focused checks that protect the touched files, then push [Task 1]
- Symptom: the repo has unrelated local changes and the user only wants a focused fix pushed. Cause: staging or committing the whole worktree would mix unrelated work into the release. Fix: keep the branch clean by staging only the intended files and preserve the rest of the worktree outside the commit [Task 1]
- Symptom: rebase conflict resolution in a minified or one-line file turns into brittle fragment editing. Cause: line-oriented patching does not match the real structure well enough. Fix: reapply the local logic on top of the upstream file as a whole and verify with focused tests before `git rebase --continue` [Task 1]
- Symptom: sync or push commands hang waiting for auth input. Cause: interactive Git prompts are blocking automation on this HTTP remote. Fix: switch to `git -c credential.interactive=never` for fetch/push and resolve the real auth or conflict issue explicitly instead of waiting on the prompt [Task 2]
- Symptom: `git pull --ff-only` cannot authenticate despite apparently clean ahead/behind output. Cause: stale Gitea OAuth credentials remain cached, and cached divergence output is not proof that fetch worked. Fix: use `git credential-manager erase`, retry the remote operation, and compare directly with `origin/main`; quote `@{upstream}` if PowerShell mangles `git rev-list --left-right --count HEAD...@{upstream}` [Task 3]
- Symptom: the first non-interactive fetch returns `Authentication failed`. Cause: stale HTTP credentials for `<internal-host>:3001`. Fix: run `git credential reject` first, then retry the non-interactive fetch before assessing branch state [Task 4]
- Symptom: `git pull --ff-only` fails with authentication even though cached divergence looks clean. Cause: stale Git Credential Manager state makes the branch evidence stale. Fix: erase the matching host credential, retry the pull, then verify final remote SHA and `0 0` divergence; do not treat a redirected restart request as completed [Task 6]
- Symptom: Git still reaches `<internal-host>` after changing `origin` to `.28`. Cause: user-level rewrite/config remains stale: `url.ssh://kele-git/.insteadof=http://<internal-host>:3001/` and SSH alias `kele-git` pointed to `.18:2222` in this run. Fix: inspect global URL rewrites and SSH config before treating repository `origin` as the remaining cause [Task 9]

# Task Group: D:\hyli\kele-0707 script-analysis description types and segment-transformation contracts
scope: Trace and safely change the `analyze_script` description-type pipeline and step-5 segment merges in `D:\hyli\kele-0707`.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse for this checkout's legacy script-analysis prompts, Worker post-processing, and Provider Gateway transform contract; re-check current prompts and consumer code before applying type or merge assumptions to another checkout.

## Task 1: Trace first/second analysis-step segment types and downstream description consumers, success

### rollout_summary_files

- rollout_summaries/2026-07-17T03-26-05-CC7X-kele_0707_script_analysis_runtime_operations.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\17\rollout-2026-07-17T11-26-10-019f6e1c-2431-7df1-9a50-0f160a3c9605.jsonl, updated_at=2026-07-20T02:42:54+00:00, thread_id=019f6e1c-2431-7df1-9a50-0f160a3c9605, traced prompt through Worker, consumers, presence reducers, and persistence)

### keywords

- analyze_script, descriptionType, analyze-script-structure-01-system-prompt, analyze-script-structure-03-system-prompt, scene-structure-analyzer.ts, enter-exit-presence-analysis.ts, opening-info, state-change, presence reducer

## Task 2: Canonicalize ordinary descriptions and constrain step-5 speech merges, success

### rollout_summary_files

- rollout_summaries/2026-07-17T03-26-05-CC7X-kele_0707_script_analysis_runtime_operations.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\17\rollout-2026-07-17T11-26-10-019f6e1c-2431-7df1-9a50-0f160a3c9605.jsonl, updated_at=2026-07-20T02:42:54+00:00, thread_id=019f6e1c-2431-7df1-9a50-0f160a3c9605, added guarded merge plans and focused contract coverage)

### keywords

- segment-transformation-contract.ts, speechMerges, sourceSegmentIds, sourceGroupId, relation: 'simultaneous', script-structure-text-provider.ts, analyze-script-description-type-contract.test.mjs, 129/129, pnpm ts-check

## User preferences

- when investigating description types, the user asked for the complete downstream use and why final scene/shot analysis is dominated by the state-change subtype -> trace prompts, Worker post-processing, scene/shot consumers, presence reducers, and persistence rather than answer from UI labels alone [Task 1]

## Reusable knowledge

- Step 1 emits dialogue-or-action, description, non-body, and a delete filtering decision; step 2 splits dialogue-or-action into dialogue or action, leaving final dialogue, action, description, and non-body types. `delete` is not an output segment type. [Task 1]
- Key consumers are `services/worker/structure/analyze-script-structure-opening-info.js`, `src/lib/scene-structure-analyzer.ts`, `src/lib/script-analysis/enter-exit-presence-analysis.ts`, and shot-splitting display/presence helpers. Ordinary descriptions do not change physical presence; only explicit enter/exit markers do. [Task 1]
- Missing or unrecognized ordinary-description subtype defaulted to the legacy state-change value, explaining its high proportion. The patch makes ordinary description canonical while accepting the legacy value on reads. [Task 1][Task 2]
- `src/lib/script-analysis/segment-transformation-contract.ts` tracks `segmentId`, `sourceSegmentIds`, and `sourceGroupId`. Step 5 now returns `speechMerges` plans; Provider Gateway allows only `relation: 'simultaneous'` and rejects cross-group, reused-ID, blocking non-description, invalid-type, and excessive-reduction merges. Invalid plans retain the original input through the error path. [Task 2]
- Focused behavioral verification passed `129/129`, `pnpm ts-check`, and `git diff --check`. A separate legacy static parity suite still had 13 pre-existing anchor/parity failures, so do not report the full suite as passing. [Task 2]

## Failures and how to do differently

- Symptom: an answer explains a description type only from UI wording. Cause: `descriptionType` changes meaning across prompts, Worker transformations, read consumers, and presence logic. Fix: trace the full producer-to-persistence/read chain before calling a subtype unused or redundant. [Task 1]
- Symptom: an LLM-directed merge silently loses content or crosses unrelated source groups. Cause: returning an unconstrained rewritten array lacks lineage and conservation checks. Fix: consume guarded `speechMerges` plans and fall back to original segments on any contract violation. [Task 2]

# Task Group: D:\hyli\kele-0707 local runtime restart, Node 26 compatibility, and restart documentation
scope: Restart and verify the complete `D:\hyli\kele-0707` local stack, including Windows Node 26 loader URLs and repository restart documentation.
applies_to: cwd=D:\hyli\kele-0707; reuse_rule=Reuse for this checkout's current `.runtime` scripts, bundled PostgreSQL, and Node 26 behavior; re-check ports, scripts, and Node version when the stack changes.

## Task 1: Repair Windows Node 26 startup and restart the complete local stack, success

### rollout_summary_files

- rollout_summaries/2026-07-17T03-26-05-CC7X-kele_0707_script_analysis_runtime_operations.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\17\rollout-2026-07-17T11-26-10-019f6e1c-2431-7df1-9a50-0f160a3c9605.jsonl, updated_at=2026-07-20T02:42:54+00:00, thread_id=019f6e1c-2431-7df1-9a50-0f160a3c9605, repaired loader URL handling and verified all runtime services)

### keywords

- restart-local-stack.ps1, Node 26, ERR_UNSUPPORTED_ESM_URL_SCHEME, ERR_INVALID_FILE_URL_PATH, cygpath -m, file:///D:/, PostgreSQL 55432, PostgREST 55437, NATS 4224, Gateway 8091, Web 3333

## Task 2: Preserve local restart changes during pull and document the manual operation, success

### rollout_summary_files

- rollout_summaries/2026-07-17T03-26-05-CC7X-kele_0707_script_analysis_runtime_operations.md (cwd=D:\hyli\kele-0707, rollout_path=\\?\%USERPROFILE%\.codex\sessions\2026\07\17\rollout-2026-07-17T11-26-10-019f6e1c-2431-7df1-9a50-0f160a3c9605.jsonl, updated_at=2026-07-20T02:42:54+00:00, thread_id=019f6e1c-2431-7df1-9a50-0f160a3c9605, preserved runtime edits through pull and added restart documentation)

### keywords

- git pull --rebase --autostash origin main, docs/LOCAL-DEV-RESTART.md, docs/README.md, scripts/dev.sh, stale credential, git credential reject

## User preferences

- when the user asked to save the manual restart instructions as a document -> preserve the operational workflow in repository docs and add a clear `docs/README.md` entry [Task 2]

## Reusable knowledge

- Related skill: skills/kele0707-full-stack-restart/SKILL.md [Task 1]
- The standard complete restart entrypoint is `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\.runtime\\restart-local-stack.ps1`, not plain `pnpm dev`. Expected ports are Web `3333`, Gateway `8091`, NATS client/monitoring `4224`/`8224`, PostgreSQL `55432`, and PostgREST `55437`. [Task 1]
- Bundled PostgreSQL binaries are under `%USERPROFILE%\AppData\Local\Codex\postgresql\17.10\pgsql\bin`; its data directory is `%USERPROFILE%\AppData\Local\Codex\kele-postgres\data`. Start PostgreSQL before PostgREST when migration startup reports `ECONNREFUSED 127.0.0.1:55432`. [Task 1]
- Node 26 on Windows rejects `/d/...` and `D:/...` supplied to `--import`. In `scripts/dev.sh`, derive a Windows path with `cygpath -m` and emit `file:///D:/...`; hardcoded `/d/...` caused `ERR_UNSUPPORTED_ESM_URL_SCHEME`, while malformed lowercase URLs caused `ERR_INVALID_FILE_URL_PATH`. [Task 1]
- Verification succeeded only after HTTP 200 from Web/PostgREST/NATS, all six listeners, and Scheduler/Worker/Projector startup or heartbeat logs. [Task 1]
- `git pull --rebase --autostash origin main` preserved local `scripts/dev.sh` changes while updating remote state. Reject stale HTTP credential entries rather than recording credentials in repo docs or memory. [Task 2]

## Failures and how to do differently

- Symptom: switching from `--loader` to `--import` still fails under Node 26. Cause: the argument is a Windows path rather than a valid file URL. Fix: generate `file:///D:/...` from `cygpath -m`, then verify all runtime roles remain running. [Task 1]
- Symptom: the web app responds but the workflow runtime remains unavailable. Cause: only a partial startup completed. Fix: use `.runtime/restart-local-stack.ps1` and require ports, HTTP health, and runtime logs together. [Task 1]
